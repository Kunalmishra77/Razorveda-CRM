# 10 — Operating Model Addendum (moved)

This file previously duplicated `docs/reference/addendum-v2.md` byte for byte, and its Phase 0 exit
criteria contradicted `tasks/phase-0-foundation.md`. Two roadmaps in one repo is how projects rot.

**The working spec is `docs/00` through `docs/09` plus `tasks/`.**
The addendum is background reading only: `docs/reference/addendum-v2.md`.

Where they disagreed, `tasks/phase-N-*.md` wins. Always.
