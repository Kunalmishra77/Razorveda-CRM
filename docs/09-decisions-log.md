# 09 — Decisions Log

Append-only. Claude Code must add an entry for any decision it makes that is not already documented.

---

## Decided

| # | Decision | Rationale | Date |
|---|---|---|---|
| D-01 | Surrogate UUID PK for customer; phone is a unique business key in `customer_identifier` | 10.9% of historical rows have no valid phone; alt numbers, shared numbers and number changes all occur in the real data | 2026-08-20 |
| D-02 | No auto-assignment. Admin bulk-assigns from a pool | Client decision | 2026-08-20 |
| D-03 | Reps dial from own handsets; no telephony integration | Client decision. Consequence: dial/connect/connectivity are self-reported | 2026-08-20 |
| D-04 | Two functional roles (ADMIN, EMPLOYEE) + one OWNER account | Client decision + governance gap noted in docs/05 | 2026-08-20 |
| D-05 | Incentive and score on **realised** (delivered) value with automatic clawback | Fixes findings F7, F10, F11 simultaneously | 2026-08-20 |
| D-06 | `Required Booking = Per Day Req ÷ (1 − rep 90d RTO)` replaces flat ×1.15 | Finding F11 — current formula understates Kajal's requirement by 47% | 2026-08-20 |
| D-07 | Product revenue allocated at `order_line` grain | Finding F8 — Skinwise reports ₹0 against ₹2.5L actual | 2026-08-20 |
| D-08 | Postgres RLS as the isolation mechanism, not application filters | ADR-001 | 2026-08-20 |
| D-09 | Append-only tables for money and status | ADR-002 — makes historical reports reproducible | 2026-08-20 |
| D-10 | Materialised views, no data warehouse | ADR-003 — 730K rows/year does not justify one | 2026-08-20 |
| D-11 | AI never computes a number, decides an assignment, or writes to the ledger | An LLM that occasionally hallucinates a total is worse than the current spreadsheet | 2026-08-20 |
| D-12 | Column-shift detector fails the whole batch at >20% type failure | Finding F3 — silent corruption went unnoticed for months | 2026-08-20 |

---

## Open — blocks Phase 0 / Phase 1

| # | Question | Blocks | Owner |
|---|---|---|---|
| O-01 | **Definitive employee roster.** Brief says 7; Achieve Report has 11 (adds Puja Singh, Mala, Priyanka, Kajal); Scoreboard has 10. `db/seed/employees.csv` is a best guess and must be confirmed. | Targets, assignment, incentive | Client |
| O-02 | **Confirm Shopify base price per SKU.** ₹899 / ₹849 / ₹949 clusters were reverse-engineered from order data. `db/seed/skus.csv` values are inferred. | Automatic upsell credit (Phase 3) | Client |
| O-03 | **Confirm `usage_days` per SKU.** Drives the repeat-purchase engine. Current values are estimates. | Repeat-due engine (Phase 3) | Client |
| O-04 | **Approve realised-basis scoring** (D-05). Changes how everyone is paid. | Phase 3 | Client |
| O-05 | **Approve rep-specific RTO buffers** (D-06). Some targets rise sharply. | Phase 4 | Client |
| O-06 | **Historical depth to migrate** — Apr–Aug 2026 only, or the full archive back to Sept 2025? | Phase 0 effort, Phase 6 model training | Client |
| O-07 | **Nominate the OWNER account.** | Phase 1 auth | Client |
| O-08 | **Working calendar** — which days are working days? Affects every "per day required" number. | Phase 0 seed | Client |
| O-09 | **Incentive slab values** — the defaults in docs/03 section 6 are proposals, not the client's actual scheme. | Phase 3 | Client |
| O-10 | **Cloud dialler in Phase 6** — yes / no / decide later. Determines whether dial metrics ever become real. | Phase 6 | Client |

---

## Session log

*(Claude Code appends here each session: date, phase, decisions made, questions raised.)*

---

# Decisions taken 20 Aug 2026 — in response to the Phase 0 kickoff audit

The engineering audit raised 15 findings (B1–B15). Verified against the files; the following are
now **decided**. Anything not listed here remains open.

*Numbered D-13 onward — the block above already uses D-01…D-12. (defect N4)*

| ID | Decision | Rationale |
|---|---|---|
| **D-13** | Booked/Realised invariant is **per order**, not per period. Incentive on **cash basis** (delivered-in-period). Cohort basis available as a secondary report. | B4 was correct and the v1 draft was wrong. See metric dictionary §6. The F10 "impossible" claim about Divya is withdrawn — it is most likely a cohort artefact. |
| **D-14** | **pgvector removed from v1.** SKU resolution uses exact → alias table → `pg_trgm` similarity → human queue. | 20 SKUs do not need embeddings, and it removes a Coolify blocker. Revisit above ~500 SKUs. |
| **D-15** | **One customer per mobile number.** `customer.primary_phone` stays UNIQUE. A different recipient name on an order is stored on the order, not as a second customer. | B9. Families sharing a number is an *ordering* fact, not an *identity* fact. Two customer records on one number would break dedupe and the <1% duplicate target. The surrogate PK is justified by blank phones, alt numbers and number changes — not by shared numbers. |
| **D-16** | `employee_credit_percent` stays 100 for RTO_RECOVERY and NC_REFUSED, **flagged as O-11** for the client. | B11. Recovering a dead order is real work, so full credit is defensible — but it is a money rule and the client has never been asked. |
| **D-17** | Dev and CI run against **local Postgres in Docker**. Coolify is the deploy target only. | Destructive schema and RLS tests must never touch the only live database. Requires Docker Desktop. |
| **D-18** | Fixtures are corrected in **Phase 0**, not Phase 2. | Phase 2 mandates writing tests from fixtures. Wrong fixtures produce tests that encode wrong expectations. |
| **D-19** | Seed roster keeps 9 employees including Megha (ON_LEAVE) and Shweta, marked provisional. | Megha powers the "rep on leave" assignment warning. O-01 stays open. |
| **D-20** | Alias test asserts **every seeded alias resolves**, plus a separate documented list of the 49 observed raw strings. | B14. "All 49 variants" was untestable as written. |
| **D-21** | The app connects as `app_role`, which **owns nothing**. CI fails if the API's `DATABASE_URL` role owns any table in `public`. | B3. Owners bypass RLS; a test run as owner proves nothing. |

## New open decisions for the client

| ID | Question | Blocks |
|---|---|---|
| **O-11** | Should RTO / NC recovery earn 100% employee credit, or a reduced percentage? | Phase 3 |
| **O-12** | Confirm the per-target-field type contracts for column-shift detection (B10) — which fields are strictly typed (pincode, AWB, enum, date, numeric) so the detector watches more than one column. | Phase 2 |

## Second correction pass — 21 Aug 2026 (defects N1–N6)

| ID | Decision |
|---|---|
| **D-22** | All RLS policies go through `current_employee_id()` / `is_admin()`. Raw `current_setting('app.user_id')` comparisons against an `employee_id` are a bug (N1). CI greps for them. |
| **D-23** | A policy on the `employee` table uses `current_user_id()`, never `current_employee_id()` — the latter selects from `employee` and recurses under FORCE RLS. |
| **D-24** | Settled decisions beat stale prose. Where a doc contradicts a logged decision, the decision wins and the doc gets corrected in the same commit (N2, N3). |
| **D-25** | Decision IDs are globally unique and never reused. Audit-response block is D-13…D-21; this block is D-22 onward (N4). |
| **D-26** | `ALTER DEFAULT PRIVILEGES` is required so future migrations inherit `app_role` grants (N5). |
| **D-27** | All seven master tables get `FORCE ROW LEVEL SECURITY` (N6). |

| ID | Open question |
|---|---|
| **O-13** | `employee.monthly_target` is column-level, which RLS cannot hide. v1 filters it in the API for non-admins. Do we want hard enforcement via an admin-only view and a column REVOKE? |

---

# Session log — 2026-08-21 · Phase 0 (part 1)

Scaffold, `packages/shared`, `packages/metrics`. Decisions taken that were not already
documented, per CLAUDE.md section 4 ("Log decisions").

| ID | Decision | Rationale |
|---|---|---|
| **D-28** | Line endings forced to LF via `.gitattributes`. | Ingestion hashes raw file bytes (docs/06 stage 1). CRLF rewriting on checkout would give the same fixture a different SHA-256 on Windows and in Linux CI, making duplicate-file detection platform-dependent. The v3 zip shipped `delivered_data_sample.csv` with CRLF, so this was already live. |
| **D-29** | Money crosses the wire as a **decimal string**, never a JS number. | `numeric(12,2)` in Postgres; docs/02 says "Never float." A JSON number is an IEEE double, so parsing a rupee value into one reintroduces exactly the drift the schema forbids. `moneySchema` enforces the string form. |
| **D-30** | Postgres enums get the **same parity guardrail** as the metric dictionary. `packages/shared/test/enum-parity.test.ts` parses `db/schema.sql` and fails on any drift. | Not requested, but the failure mode is identical to the one D-20 and docs/03 rule 3 exist to prevent: a value added in SQL that silently never reaches the API and web. 15 enums, 18 assertions. |
| **D-31** | A metric's identity in the registry is **(section, name)**, not name alone. | "Upsell Index" legitimately appears in both docs/03 section 4 (attribution metric) and section 5 (EES score component). Keying on name alone would have made one of them invisible to the parity test. |
| **D-32** | The parity test **guards the guard**: it asserts the doc parser found >20 metrics across all five sections. | A parser that silently matches nothing makes every other assertion in the file pass vacuously. That is the classic way a guardrail rots without anyone noticing. |
| **D-33** | Local `docker-compose.yml` runs Postgres as `razorveda_migrator`, not `postgres`. | Implements D-21 in dev, not just in prod. The owner role and the app role must be different locally too, or the isolation test proves nothing on the machine where it is actually run. |

## Raised this session, not decided

| # | Observation | Status |
|---|---|---|
| **N7** | `docs/03-metric-dictionary.md` has **two headings numbered `## 6.`** — "6. Incentive" (line 114) and "6. Period basis" (line 133). The second is presumably section 7. | Flagged, not fixed. Harmless today because neither is a metric table, but the parity parser keys section context off the heading number, so a metric table added under the second `## 6.` would be filed under section 6 and mismatch the registry. Client/author call. |

---

# Decisions taken 2026-08-21 — third pass

| ID | Decision | Rationale |
|---|---|---|
| **D-34** | **O-08 resolved.** Sundays non-working, confirmed by reproducing the client's forward denominator exactly (`153,769.39 / 12 = 12,814.11583`, matching the sheet to five decimals). **Both** denominators come from `working_calendar` — no hand-typed day counts anywhere. Festival holidays seed **empty**, admin-toggleable, provisional until the client confirms. See metric dictionary section 8. | The forward rule was verifiable from the client's own numbers, so Sundays-off is confirmed by data rather than assumed. The backward rule was not reproducible by any calendar — see F17. |
| **D-35** | Section numbers in `docs/03` are **unique and sequential**, asserted by its own test (`packages/metrics/test/section-numbering.test.ts`) with its own failure message. The parity parser should eventually key off a stable slug, not the digit. | N7. A duplicate number does not fail loudly — it files a metric under the wrong section, and parity then reports a *missing metric*, sending the next person to edit the registry when the defect is a heading. The separate test fails first and names the document, the line numbers and the fix. |
| **D-36** | Corrections ship as **instructions or file patches over an existing tree**, never a folder replacement. | A folder replacement destroyed `.git` and `.gitattributes` once already, silently. Nothing in the working tree should be able to disappear without a commit recording it. |
| **D-37** | A markdown table in `docs/03` whose first header cell is `Metric` or `Component` **is** a metric-definition table, by parser contract. Denominator rules, levers, bases and other prose tables must use a different header or a code block. | Caught immediately by the parity test while applying D-34: rendering the section 8 denominators as a `\| Metric \|` table added three phantom metrics (39 -> 42). The parser was right; the formatting was wrong. Documented so the next person renders it correctly the first time. |

## New finding

| ID | Finding | Evidence | Design response |
|---|---|---|---|
| **F17** | **`Per Day Avg Value` divides by a hand-typed 11 where the calendar gives 14.** Every rep's "Approx Guess Rest of Month" is overstated by roughly a third. | Working days 1–17 Aug 2026 with Sundays off is 14. No calendar rule produces 11. Nikita: `146,230.61 / 11 = 13,293.69 -> x12 = 1,59,524` against the calendar-correct `146,230.61 / 14 = 10,445.04 -> x12 = 1,25,340`. A **₹34,184 over-forecast on one rep.** | Both denominators read `working_calendar` (D-34). This is the mechanism behind the over-forecasting recorded in F16 — the v1 audit blamed straight-line extrapolation, which was true but smaller than the wrong divisor. Carry into the Phase 2 variance report citing metric dictionary section 8. |

## Raised this session, not decided

| # | Question | Status |
|---|---|---|
| **N8** | Are `Per Day Avg Value` and `Approx Guess Rest of Month` **certified metrics** that belong in docs/03 section 3 with registry entries, or legacy formulas superseded by `Forecast`? Section 8 now specifies how to compute both, but neither is defined in section 3 and neither is in the registry. Under docs/03 rule 1 ("if a metric is not in this file, it does not exist and no screen may display it") they currently cannot be displayed. | Flagged, not decided. `Forecast` (section 3) is their stated replacement per F16, so the honest reading is that they are legacy — but section 8 gives them live computation rules, which reads as though they survive. Needs one line either way. |

---

# Decisions taken 2026-08-21 — fourth pass

| ID | Decision | Rationale |
|---|---|---|
| **D-38** | **N8 resolved by splitting.** `Per Day Avg Value` is registered **live**; `Approx Guess Rest of Month` is registered **legacy**; `Forecast` is redefined as the live replacement. `MetricDef` gains `status: 'live' \| 'legacy'`. | They looked like one question because they sit in the same sheet column group. One is a sound run-rate whose only defect was the hand-typed divisor; the other is the straight-line formula F16 exists to replace. A legacy metric is **recorded, not omitted** — an "it is documented but not a metric" category outside the dictionary is how the dictionary starts rotting. Rule 1 holds unbroken. |
| **D-39** | **Exact arithmetic, rounded once at render.** No metric consumes another metric's displayed form. docs/03 section 9. | The hand-typed `11` (F17, ₹34,184) and the multiplied-display `10,445.04` (₹1) are the same defect at different magnitudes: a rounded intermediate escaping into a computed result. Enforced as two lint assertions on registry view SQL, plus a third that records they are inert until Phase 4 views land so they cannot rot into decoration. |
| **D-40** | **`_local_dev_marker` sentinel.** `migrate --fresh` creates it; `seed` and `migrate --fresh` refuse without it. Host check retained alongside. | The Tailscale form of the production URL is `127.0.0.1:55432` — loopback — so host-matching is decorative against the person most likely to trip it. The marker is independent of the URL: production has tables and no marker, so it fails closed. Two cheap checks, neither sufficient alone. Residual gap stated in source and README: this guards a stray `DATABASE_URL` during a seed, not a determined operator. |
| **D-41** | **No forced-reset column.** The un-nominated OWNER account is seeded `is_locked = true` with `locked_reason` naming O-07. | `app_user` has no such column, and adding one to hold an open business decision is worse than using the lock that already exists. docs/05 test 8 already requires that a locked account cannot authenticate until an admin unlocks it — exactly the semantics wanted, and stronger. Claiming the account is set-email plus unlock. **Do not "add the missing flag" later.** |
| **D-42** | **Argon2id parameters pinned in `packages/shared`** (m=19456, t=2, p=1 — OWASP minimums), asserted against a real hash in `packages/db`. | A library swap must not be able to change the cost factor quietly. `@node-rs/argon2` loaded its prebuilt binary with no native build (43 ms, `$argon2id$` confirmed), so no fallback was needed — but the assertion is what makes that durable. Never bcrypt, never a silent fallback. |

## Raised this session, not decided

| # | Question | Status |
|---|---|---|
| **N9** | **`stage_probability` has no defined stage vocabulary.** `Forecast` multiplies `open_pipeline × stage_probability`, and the probabilities must come from a seeded table rather than constants. But no document defines what a *pipeline stage* is. `buyer_stage` is customer lifecycle (PROSPECT…CHURNED); `disposition` is contact outcome; neither is a pipeline stage with a conversion probability. | **Flagged, table not created.** Seeding one would mean inventing a stage vocabulary, and CLAUDE.md §4 forbids guessing a business rule. The registry records that probabilities are data, not constants, so nothing is hardcoded in the meantime. Needs a stage list before the table can be seeded from `expected_conversion_rate`. Forecast is Phase 4/6, so this is not urgent. |
| **N10** | **The revised `Forecast` drops `seasonality_index`.** The previous definition was `(open_pipeline × stage_probability) + (run_rate × remaining_days × seasonality_index)`, RTO-adjusted. F16's design response in docs/08 says "pipeline-weighted forecast **with seasonality** and RTO adjustment", and docs/10 Part 4.7 repeats it. The new formula has no seasonality term. | **Flagged, implemented as specified.** Deliberate simplification or an oversight? A pan-India ayurvedic D2C business plausibly has festival seasonality, which is also why O-08's holiday list matters. One line either way; docs/08 F16 should be amended if the drop is intended. |

---

# Decisions taken 2026-08-21 — fifth pass

| ID | Decision | Rationale |
|---|---|---|
| **D-43** | **N9 resolved: the stage IS the disposition.** No new pipeline-stage vocabulary. `stage_probability(disposition)` = delivered orders ÷ leads that passed through that disposition, over a rolling window, materialised nightly from the order ledger. Below **30** leads, fall back to `lead_source.expected_conversion_rate`. Every forecast output carries `forecast_weight_source`. | A parallel vocabulary would give the system two ways to say where a lead sits. The disposition table has been the stage list all along. The 30-lead floor is the same shrinkage instinct as the EES score's k=30 — a rate computed from nine leads is noise wearing a decimal point. No new table, no invented numbers, self-correcting as history accumulates. docs/03 §10.1. |
| **D-44** | **N10 resolved: keep the term, neutralise the value.** `seasonality_index` stays in the Forecast formula and is seeded **1.0000, provisional, for all twelve months** in a new `seasonality_index` table. Revisit at 18+ months of history, or sooner if O-06 releases the 2025 archive. No screen may describe the forecast as seasonally adjusted while this holds. | Dropping it silently was wrong — F16 calls for seasonality. But it cannot be fitted on five months (Apr–Aug): an index fitted on that little data looks principled while encoding noise, which is worse than none. **A formula silently missing a term is far harder to find later than one carrying an obvious 1.0.** Values live in a seeded, admin-editable table, never as a constant. docs/03 §10.2. |
| **D-45** | New master tables join the master-data RLS loop (`ENABLE` + `FORCE`, read-all, admin-write) as a matter of course, and the coverage check is re-run after any schema change. | `seasonality_index` was added this session. All 26 tables carry RLS; the count is verified, not assumed. |

---

# Session log — 2026-08-21 · Phase 1 (part 1: auth + RLS)

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-46** | The RLS transaction issues `SET LOCAL ROLE app_role` **before** setting any value, and binds `app.user_id` / `app.user_role` via `set_config(..., true)` rather than `SET LOCAL <name> = '<literal>'`. | 1 | Two reasons. Owners bypass RLS, so switching role inside the transaction means a misconfigured `DATABASE_URL_APP` becomes a permissions error rather than a silent data leak. And `SET LOCAL` takes no bind parameters, so the literal form would mean interpolating a value into SQL on the authentication path; `set_config` is the parameterised equivalent. |
| **D-47** | The eight isolation tests live in a **separate suite** (`npm run test:rls`) that **fails, never skips**, when no database is present. | 1 | A suite that skips on a missing database turns "we never ran it" into "it passed". For RLS — the one thing in this system that fails silently — that is the worst possible default. Verified: with no `DATABASE_URL` the runner exits 1. |
| **D-48** | Isolation test 3 asserts a rep **can** read their own customers' phone numbers, not only that they cannot read others'. | 1 | N1 was a fail-*closed* bug: the policies compared an `employee_id` to a `user_id`, so every rep saw zero rows including their own. A test that only checks for absence would have passed while Lead Detail was broken. |
| **D-49** | Isolation test 8 runs the same query **as the table owner, without `SET ROLE`**, and asserts the count is HIGHER. | 1 | It is the test that stops the other seven lying. If those counts are ever equal, either isolation is broken or the seed has no cross-rep data — both worth failing for. |
| **D-50** | TOTP implemented on `node:crypto` and verified against the **published RFC 4226 and RFC 6238 test vectors**, rather than taking a dependency. | 2 | TOTP is HMAC plus a truncation rule, not a primitive being invented, and the RFC publishes official vectors — so this can be checked against the specification instead of trusted. Rejected `otplib`: perfectly reasonable, and swapping is small since the surface is two functions. 23 assertions, all RFC vectors. |
| **D-51** | Login check order is fixed: **password → locked → shift → TOTP**, and it is a security property, not style. | 2 | Every reason after the first is more specific than "no". Revealing one before the password is verified turns the login form into an account-enumeration oracle — which addresses exist, which are locked, who works which shift. Asserted: a locked account with a wrong password reports only `INVALID_CREDENTIALS` and never leaks the lock reason. |
| **D-52** | An ADMIN or OWNER with **no enrolled TOTP secret is refused**, not waved through. | 2 | The dangerous branch is mandatory 2FA that silently becomes optional because enrolment was never finished. Fails safe and is cheap to reverse. Rejected: allowing first-login enrolment, which would leave a window where an admin password alone is sufficient. |
| **D-53** | `isWithinShift` supports a window crossing midnight. | 2 | The seeded 10:00–20:00 does not need it, but a night shift would, and discovering that by locking a team out is an expensive way to find the bug. |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-54** | Refresh rotation carries **reuse detection**: presenting a superseded token revokes the whole session. Expiry is checked before reuse. | 2 | Rotation alone is not a control — a thief and the real user would take turns rotating. Detecting a stale token makes rotation a tripwire: a stolen token buys one use. Expiry first, so an ordinary lapsed login is not reported as an attack; training people to ignore security events is its own vulnerability. |
| **D-55** | Single active session enforced for **EMPLOYEE only**. ADMIN and OWNER exempt. | 2 | Three admins share upload and review duties across machines; a one-device rule there would be worked around rather than followed. Reps are the exfiltration surface the control exists for (docs/05). |
| **D-56** | Identity resolution does **not** compute name similarity. `pg_trgm` scores are passed in; this module is the pure decision over scored candidates. | 1 | Reimplementing trigram scoring in TypeScript would create a second source of truth for the most consequential number in dedupe, and the two would drift the first time either changed. No drift is possible by construction. |
| **D-57** | A fuzzy identity match requires **both** name ≥0.85 **and** a matching pincode. Name alone never merges. | 1 | docs/06 §4 states both. Worth restating because the client's data is full of single-word names — "Aarti" in Meerut and "Aarti" in Mysuru are different people, and silently merging two real customers is far more expensive to undo than reviewing 26 rows. |
| **D-58** | Money arithmetic uses exact scaled-integer BigInt internally, never JS floats; `splitMoney` uses **largest-remainder** so parts always re-sum to the whole. | 1 | Implements D-29 and D-39. Rounding each share independently loses paise: ₹1,000 across 33.33/33.33/33.34 gives ₹999.90. An attribution ledger that does not add up is worse than one a paisa unfair to one rep. Scaling to integers is a computation detail — nothing is stored or transmitted as paise (docs/02). |
| **D-59** | `company_base_value` **throws** when a non-upsell line has no `shopify_base_price`, rather than defaulting to zero. | 2 | Defaulting to zero credits the rep the entire order value — F7 reproduced faithfully in code. Failing loudly routes the row to the exception queue where a human decides. Expect this to fire on real data until O-02 confirms the base prices; that is the intended behaviour, not a defect. |
| **D-60** | A base above the final value **clamps** the credit to zero rather than paying negative. | 2 | The rep discounted below the committed cart. A clawback is a ledger event, not an arithmetic accident. `ruleApplied` records `_CLAMPED` so the case is visible in the ledger rather than indistinguishable from a zero-credit order. |
| **D-61** | Order-status transitions are guarded by an explicit table; NDR states are **not** terminal. | 2 | docs/ specifies the statuses and the clawback rule but not the graph, so this encodes the courier lifecycle a pan-India COD business actually has. FAILED_DELIVERY, NO_RESPONSE and REFUSED can all return to OFD, which is the whole reason the RTO_RECOVERY and NC_REFUSED channels exist. |
| **D-62** | `ledgerEffectOf` returns `NONE` for any **illegal** transition, and a clawback fires only on a transition **out of DELIVERED**. | 1 | Found by a test, not by review: the first version keyed purely on the destination, so an illegal `PENDING -> DELIVERED` still returned `REALISED_CREDIT`. A caller computing the effect before asserting the transition would have written money against an order that was never dispatched — into an append-only ledger, so uncorrectable by edit. The clawback rule matters equally: an order that goes straight to RTO never realised, so reversing it would double-count the loss. |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-63** | `connected` is true only when the rep ticks it **and** the disposition's `counts_as_connect` agrees. | 1 | "Ringing" with connected ticked is a mis-click, not a conversation. The disposition is the closed vocabulary (F4); the checkbox is self-reported (D-03). Trusting the checkbox alone would inflate CD, which the client reads daily. |
| **D-64** | `NOTE` and `SYSTEM` activities do **not** increment `contact_attempts` and do not require a disposition. | 1 | Fq is a real metric in the client's MIS, not a row counter. A note about a customer is not a contact attempt. |
| **D-65** | A terminal disposition clears `next_followup_at` and stamps `closed_at`, and a closed lead leaves the worklist entirely. | 2 | 174 client leads sat with a rep for a full validity window producing nothing. A dead lead that keeps claiming the top of the worklist is how that happens. `closed_at` is set once and never moved by a later activity. |
| **D-66** | `remark_raw` is stored byte-for-byte apart from trailing whitespace. No case change, no spell correction, no transliteration. | 1 | docs/07 §4. Four months of Hinglish remarks are the raw material for Phase 6 objection intelligence; normalising on the way in destroys the evidence and leaves only a guess at what was meant. |
| **D-67** | Pre-assign warnings are ordered **destination-rep first**, then leads. | 2 | An admin reads the first line and little else. "Megha is on leave" is more actionable than "4 of these are past validity". |
| **D-68** | The Suggested Split's new-joiner yield floor is **relative to the team median (25%)**, not an absolute constant. | 2 | Found by a test. The first version floored yield at 0.1, which against a team yielding ₹800/lead is numerically indistinguishable from zero — so a new joiner was proposed nothing, never built a record, and stayed at nothing. Only a relative floor self-corrects. Rejected: excluding zero-yield reps entirely, which would force an override on every assignment involving a new joiner. |
| **D-69** | The Suggested Split uses largest-remainder and **stops short rather than exceeding a WIP cap**. | 2 | An admin told to assign 59 of 60 stops trusting the button. Where the team is genuinely capped the shortfall must be visible, not hidden by pushing reps past their caps. |
| **D-70** | Worklist bands are evaluated in priority order and the **first match wins**; a lead that is several things at once is ranked by its most urgent truth. | 1 | docs/07 §4 fixes the order and forbids user sorting. A repeat-due customer whose follow-up is also overdue belongs in Overdue: a promise the rep made beats a system-generated nudge. |
| **D-71** | A follow-up promised earlier **today** stays in "Due today", not "Overdue". | 2 | A 09:00 promise at 11:00 is still today's work. Promoting it mid-morning would make the top of the list churn all day, and the band is a plan for the day rather than a stopwatch. |
| **D-72** | Ageing leads sort by **closest to validity expiry**, not by age. | 2 | Leads decay against Data Valid Till, which most CRMs never model. A lead expiring tomorrow deserves the last chance ahead of an older one with three weeks left. |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-73** | The table selection model lives in `packages/shared` as pure functions, not inside the React component, so **exit criterion 3 becomes a test** instead of "manual check, documented". | 1 | Applying the standing default: if something is untestable as specified, make it testable and say what changed. A manual check is performed once and assumed forever. 20 assertions now cover shift-click, including the range-deselect case nobody thinks to ask for. |
| **D-74** | "Select all in filter" stores **exclusions**, and the client sends `{mode: 'FILTER', excludeLeadIds}` rather than a list of ids. The server **re-runs the filter inside the assigning transaction**. | 1 | The browser holds 25 of 486 rows, so it cannot send the ids. Re-running server-side also means the set assigned is the set that exists at commit time, not a snapshot the admin loaded ten minutes ago — an ingestion batch may have added rows and another admin may have taken some. The returned count reports what actually happened. |
| **D-75** | Bulk assign uses **set-based statements** (`FROM unnest($1::uuid[])`), not a loop, and runs in one transaction. | 1 | Exit criterion 2 is 200 leads under 2 seconds. One round trip regardless of batch size makes that comfortable rather than marginal, and one transaction means a partial assignment cannot exist. The service returns both the leads moved and the `lead_assignment` rows written so a caller can assert they match — if those diverge, the append-only history no longer explains the current state. |
| **D-76** | `assigned_to IS NULL` is hardcoded into the pool query and is not a caller-supplied filter. | 1 | The absence of an assignment IS the pool (docs/02). Allowing a caller to drop that clause would let a bulk assign silently steal leads another rep is already working. Transfers are a separate, explicit action with their own `assign_method`. |
| **D-77** | The API loads the `disposition` rule from the master table and ignores any client claim about whether a follow-up is required. | 1 | Exit criterion 4 requires the API to reject, not only the UI to block. A closed vocabulary enforced in the browser is a free-text field with a nicer widget — which is what F4's 49 spellings look like after four months. |
| **D-78** | `DATABASE_URL_APP` has **no fallback** to `DATABASE_URL`; the API refuses to start without it. | 1 | A fallback would be the single worst failure mode in this codebase: connecting as the migration user disables every RLS policy while leaving all eight isolation tests green, because those tests `SET ROLE` explicitly. Failing to boot is loud; silent bypass is not. |

---

# Decisions taken 2026-08-21 — sixth pass (Phase 1 verification)

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-79** | **Local Postgres without Docker.** Real PostgreSQL **16.14** binaries via `@embedded-postgres/windows-x64`, run against a repo-local `.pgdata`. Supersedes the Docker half of D-17; Coolify still runs Docker in production. | 2 | The client's machine does not run Docker Desktop and will not — it slows the machine, which is a fair reason. These are real binaries, not an emulator and not `pg-mem`: RLS, FORCE RLS, roles, triggers and pgcrypto behave exactly as in production. The one thing in this system that fails silently must never be tested against a lookalike. Pinned to **16** to match Coolify rather than taking the latest 18, so dev and prod cannot drift. |
| **D-80** | `db:seed:dev` is a **separate** script from `db:seed`, guarded by both the host check and the sentinel. | 1 | `db:seed` loads master data — things that are true about the business. Fixture customers and leads must never reach production, and mixing them into one command is how that happens. |

## Defects found by running it — none of which review would have caught

| # | Defect | Where |
|---|---|---|
| **R1** | `working_calendar.month_key GENERATED ALWAYS AS (to_char(calendar_date,'YYYY-MM'))` — **rejected by Postgres**: `to_char(date,text)` is STABLE, not IMMUTABLE, because it depends on `DateStyle`. Rebuilt from `extract()` + `lpad`, which are immutable. Would have failed identically on Coolify. | `db/schema.sql` |
| **R2** | `npm run db:migrate -- --fresh` **silently dropped the flag**: npm consumed `--fresh` as its own option. Root scripts now end with `--` to forward arguments. The flag had never worked. | `package.json` |
| **R3** | A plain `migrate` on an empty database left **no `_local_dev_marker`**, so every later `--fresh` refused forever. The guard was right; the bootstrap path was incomplete. Migrate now marks any database it created from empty. | `packages/db/src/migrate.ts` |
| **R4** | `pg_ctl -w start` **never returns on Windows**, and `spawnSync` blocks on stdio the server holds open, and `pg_ctl` passes its **console** to the server so a `timeout` wrapper's Ctrl-C killed the database mid-`CREATE DATABASE` (exit `0xC000013A`). Now spawns `postgres.exe` directly, detached, with a real protocol handshake for readiness rather than trusting the pid file. | `packages/db/src/local-pg.ts` |
| **R5** | An append-only check on an **empty table passes vacuously** — a `FOR EACH ROW` trigger cannot fire on zero rows, which briefly read as a failing check when it was an empty fixture. All six tables now carry a row before being tested; 12/12 mutations blocked. | transcript + `seed-dev-fixture.ts` |

---

# Decisions taken 2026-08-21 — seventh pass

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-81** | **O-02 resolved as a MECHANISM, not a list.** The admin fills Shopify base prices in the Master Data panel. `sku` gains `shopify_base_price_confirmed`, `shopify_base_price_set_by` and `shopify_base_price_set_at`; **attribution refuses to compute from an unconfirmed price.** The seeded ₹899 / ₹849 / ₹949 remain as visible SUGGESTIONS. | 3 → answered | The client's answer was "price will fill by admin in admin panel", which resolves *who* supplies the number but not *what happens meanwhile*. The seeded values were reverse-engineered from historical orders — they are guesses, and this column decides what a rep is paid. CLAUDE.md rule 1: never guess a money figure. Separating the number from its provenance means the admin sees a helpful starting point while no money is ever computed from an unverified guess. An unconfirmed price sends the order to the exception queue, where the admin confirms and retries — that firing is the workflow starting, not a defect. |
| **D-82** | Re-running `db:seed` **never overwrites a confirmed price** with the inferred one. | 1 | Otherwise a routine seed would silently replace a real number an admin stood behind. Master data the admin owns must survive a seed. |

## Defects found by running it (continued)

| # | Defect | Where |
|---|---|---|
| **R6** | Adding `shopify_base_price_set_by uuid REFERENCES app_user(user_id)` inline to `sku` created a **forward reference** — masters are ordered by dependency and `sku` is declared before `app_user`. Postgres: *"relation app_user does not exist"*. The FK is now attached with `ALTER TABLE` after both exist. | `db/schema.sql` |
| **R7** | The cluster killed by R4's Ctrl-C could **not recover** — "shutting down due to startup process failure" — and had to be destroyed and rebuilt. The detached-spawn fix prevents a recurrence; the damaged cluster could not be repaired. Worth knowing that a console signal to a Windows Postgres is not a clean stop. | `.pgdata` |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-83** | **O-12 resolved.** Per-target-field type contracts written into docs/06 section 5.1: PHONE, PINCODE (6 digits, first digit 1-8), AWB (numeric 9-16), ENUM, DATE, MONEY, INTEGER, FREE_TEXT. | 3 → answered | Owed from the B10 exchange and not delivered until now. The detector is only as deep as these contracts: without them it watches one column, because a PIN code in a free-text field passes every check. That is exactly how F3 survived for months. |
| **D-84** | **FREE_TEXT columns get a heuristic, not a type check**: flag when >60% of non-empty values parse as a PIN code, AWB, phone or bare number. | 2 | A free-text column cannot fail a type check by definition, which is what made `Client Category` invisible. The 60% threshold is deliberately looser than the 20% type threshold — a real category column occasionally holds "2 Pack", and an admin who learns to click through a false alarm has lost the entire control. Typed and enum columns are the primary signal; this is the backstop. |
| **D-85** | Empty cells are **never** counted as type failures. | 1 | A sparse column is a completeness question, not a type question. Counting blanks as failures would reject every file with an optional column — `Alt number` is empty on most rows of every fixture. |
| **D-86** | The 20% rule is **more than** 20%, not "at least". | 1 | 2 failures in 10 is ordinary dirty data and the exception queue exists for it. Asserted at both edges so the boundary cannot drift silently. |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-87** | **O-01 resolved as a mechanism.** Admins manage the roster in the Employees panel; the seeded 9 are a starting point, not a claim. **Targets and roles stay OWNER-only.** | 3 → answered | Waiting for a definitive list was always the wrong shape of answer — the brief says 7, the Achieve Report 11, the Scoreboard 10 (F13), and the real roster changes as people join and leave. What matters is the boundary: docs/05 gives OWNER exactly three extra powers, and targets is one of them. An admin who could set targets could set their own incentive, so that is enforced in code rather than assumed. |
| **D-88** | **O-07 resolved as a one-time claim** gated on `OWNER_CLAIM_TOKEN`, provisioned out of band. An unset token means the account **cannot** be claimed, never "skip the check". | 3 → answered | The obvious implementation — let an admin unlock it in the panel — destroys the thing OWNER exists for. docs/05: "three admins with identical, mutually unrevocable access and nobody above them is a gap." If any admin can promote themselves, the gap is back while looking closed, which is worse than leaving it open honestly. An admin with database access could still do anything; this is the boundary that holds day to day. `ALREADY_CLAIMED` is reported before the token is checked, so a wrong token cannot probe availability. |
| **D-89** | Claiming does **not** enrol TOTP. | 1 | OWNER requires 2FA (docs/05) and `evaluateLogin` already refuses an OWNER with no enrolled secret (D-52). Enrolment is therefore forced by the login path rather than by a reminder someone can ignore. |
| **D-90** | `EXITED` returns open leads to the pool and demands a handover note; `ON_LEAVE` and `SUSPENDED` leave the pipeline intact. | 2 | docs/05 offboarding in one action. A departed rep's leads left assigned is how 174 client leads sat untouched for a full validity window. But a rep back next week should find her pipeline where she left it, and scattering a suspended rep's pipeline is hard to undo — the assignment console already warns on both. |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-91** | Column mapping runs **saved template → deterministic alias dictionary → AI**, in that order. The dictionary is a plain list of the client's known header variants. | 1 | F6's variants — `Number`/`Phone no`/`Phoneno`, `Product detail`/`ProductDeatil`, `Agent`/`CallerName` — are a list, not a language problem. Verified: **every column of all seven fixture files maps with no AI at all.** ADR-004 holds by construction, because the AI step can be removed entirely and ingestion still completes. |
| **D-92** | An **AI-mapped column always requires an admin to confirm**, whatever confidence it claims. `canAutoApply` returns true only when every column came from the dictionary. | 2 | docs/06 sets a 0.9 floor for auto-applying, but a saved template is permanent and reused every day thereafter. The cost of a wrong template is months of silently mis-mapped data; the cost of one confirmation click is a few seconds. |
| **D-93** | The B8 deny-list is a **hard block**, re-applied to AI output. `Final amount` can never map to `final_value`. | 1 | The sheet's "Final amount" is the manually typed employee credit — the words are inverted. A mapper matching on "final" corrupts every historical order and quietly changes what every rep is paid. Confidence is no defence against being confidently wrong about which column is the money. |
| **D-94** | Two columns claiming the same target field leaves **both unmapped**, rather than picking one. | 2 | `Phone no` and `Number` in one file means somebody merged two exports. Choosing silently would drop half the numbers, and the file genuinely needs a human to look at it. |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-95** | Raw uploads go to a **content-addressed store with no update and no delete**. `LocalFileStorage` for dev, MinIO on Coolify, one interface. | 2 | ADR-005 says uploads are immutable and retained so any batch can be replayed after a rule fix. That is only true if overwriting is *impossible* rather than discouraged, so the operations do not exist. The path IS the SHA-256 already computed for the duplicate check, which gives a second independent duplicate signal — the same two-cheap-guards pattern as D-40. MinIO is a Docker service, so dev uses the filesystem for the same reason Postgres runs from npm binaries (D-79). |
| **D-96** | Integrity is verified **on read and on repeat write**, not assumed. | 1 | A corrupted archive must surface now, not during a replay months later when nobody remembers what the batch contained. |
| **D-97** | Upload **hashes and refuses duplicates BEFORE storing anything**. | 1 | Storing then checking would leave an orphan file behind every re-upload, and the entire point of a duplicate refusal is that nothing happens. |

## Defects found by running it (continued)

| # | Defect | Where |
|---|---|---|
| **R8** | `pg:start` returned as soon as the socket answered an SSLRequest, but the postmaster answers that **while still starting up** — so the next command died with `57P03 the database system is starting up`. Readiness now means a completed `SELECT 1`, not an open socket. After an unclean shutdown, recovery takes seconds. | `packages/db/src/local-pg.ts` |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-98** | Row-status precedence is **PARKED > ERROR > DUPLICATE > WARNING > VALID**, and lower-severity problems are still recorded on the row. | 2 | An un-keyable row is un-keyable first: the admin's next action is to find a number, not to fix the amount, and leading with the amount would send them down the wrong path. The other issues stay attached so nothing is lost. |
| **D-99** | Pincode-to-state is **reference data, deliberately partial**, and an unknown prefix produces **no opinion**. Mismatches are WARNING, never ERROR. | 2 | Geography is a hint, not a gate — blocking on it would reject real orders, and the client's own Client Category column already holds stray PIN codes. A false warning is worse than silence: it trains admins to ignore the column. |
| **D-100** | A **9-prefix PIN is Army Postal Service**, flagged as a dispatch risk rather than a typo. | 2 | Found by a test I had written wrongly. It is structurally valid, not an unused zone, and many couriers will not deliver to an APO address — so it is a dispatch problem waiting to happen, not bad data. A military family is still a customer, so it is never rejected. docs/06 §5.1 corrected. |
| **D-101** | Value sanity has **no opinion when the SKU did not resolve**. | 1 | No MRP means no bound. Inventing one would reject real orders from products we simply failed to match. |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-102** | **`ROLLBACK BATCH` is compensating, never destructive.** Orders are CANCELLED with a new status event, credit is reversed with a ledger ADJUSTMENT, leads are CLOSED, and derived customer state is recomputed. Nothing is deleted. | 2 | The schema settled it rather than taste: `order_status_event` has no `ON DELETE CASCADE` and its DELETE is trigger-blocked, so an order with history physically cannot be deleted. That is right — CLAUDE.md rule 2 says corrections are new rows, and a rollback is a correction. A March report stays reproducible in December, including the fact that a batch was loaded and undone. |
| **D-103** | Rollback does **not** delete untouched leads, and `app_role` does **not** gain a DELETE privilege. | 2 | The first version deleted them and hit `permission denied for table lead` — `rls-policies.sql` revokes DELETE on all tables. That revoke is broader than its append-only justification, but the fix is not to widen it: closing achieves everything deleting would, because a closed lead is excluded from both the pool query and the worklist. Deleting would have bought tidier rows for a destructive privilege the app has never needed, and "the rollback needed it" is exactly how such a grant creeps in. |
| **D-104** | A batch that has **dispatched or delivered** orders cannot be rolled back at all. | 2 | A shipped parcel is not undone by an admin clicking undo. Those orders are corrected individually, and the refusal says so. |
| **D-105** | The duplicate-file index is **partial**: `UNIQUE (file_hash) WHERE status <> 'ROLLED_BACK'`. | 1 | Its job is to stop an export being COUNTED TWICE. A rolled-back batch was un-counted, so re-uploading is not a double count. A plain UNIQUE made the refusal message a lie — it tells the admin to "roll back batch X first if you meant to replace it", and rolling back would not have helped because the hash was still taken. Found by running the whole path. |
| **D-106** | A rolled-back batch can never be **re-committed**. | 1 | Committing again would recreate every lead the rollback closed and double-count the same orders in the customer aggregates. The file is uploaded again instead, which D-105 now permits. |
| **D-107** | Derived customer state is **recomputed from the order table**, never adjusted by a delta. | 1 | A delta is only correct if every previous delta was correct, and it drifts silently the first time one is missed. Recomputation is idempotent, so commit and rollback share it and running it twice changes nothing. |
| **D-108** | `RowStatus` reuses the Postgres enum mirror rather than redeclaring the same five values. | 1 | The compiler caught the duplicate export. Two definitions of a row's status is exactly the drift the enum-parity guardrail exists to prevent. |

## Defects found by running it (continued)

| # | Defect | Where |
|---|---|---|
| **R9** | `start()` treated "running but not ready" as "not running" and **spawned a second postgres against a live data directory** while the first was recovering — `lock file "postmaster.pid" already exists`. The second exits harmlessly, but two servers on one cluster is not something to leave to luck. It now waits for a live postmaster instead of starting another. | `packages/db/src/local-pg.ts` |

---

# Decisions taken 2026-08-21 — the admin path wired end to end

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-109** | Access tokens use the **`jose` library**, not a hand-rolled JWT. | 2 | TOTP was worth building from the RFC — HMAC plus truncation, with published test vectors. JWT is not that: its failure modes are alg-confusion, `alg: none` acceptance and `kid` handling, none of which appear in a happy-path test. A library that has been attacked is worth more than one I can read in an afternoon. Algorithms are pinned explicitly on verify. |
| **D-110** | Tokens live in **HttpOnly cookies**, never in a response body. The refresh cookie is scoped to `/auth/refresh` only. | 1 | docs/05 forbids API tokens for the EMPLOYEE role, and a token in `localStorage` is an API token any script on the page can take. Path-scoping the refresh cookie means an XSS anywhere else cannot reach the long-lived credential. |
| **D-111** | `SessionGuard` is registered **globally**; routes opt OUT with `@Public()`. | 1 | The opposite default means one forgotten decorator silently exposes customer data — and forgetting is the failure mode this codebase is built around. Only `/health`, `/metrics/registry` and `/auth/login` are public. |
| **D-112** | Every Nest constructor uses **explicit `@Inject()` tokens**. | 1 | esbuild — which `tsx` uses — does not implement `emitDecoratorMetadata`, so `design:paramtypes` is never written and reflective DI resolves every parameter to `undefined`. It fails at REQUEST time, not at boot, so it looked like a guard bug rather than a build one. Explicit tokens work under any transpiler. |
| **D-113** | `audit_log` and `pii_access_log` are **write-open, read-admin**. | 3 | They were admin-only for ALL operations, which was wrong twice. A LOGIN ATTEMPT has no session, so `is_admin()` is false and the attempt could not be recorded — an audit log that refuses the writes it exists to capture is not an audit log, and a failed login from an unknown address is the row you most want. Separately, docs/05 requires every copy-number action to write a row, and the people copying numbers are REPS — admin-only INSERT would have silently disabled the copy-velocity lock. Reading stays admin-only, which is the half that protects anything. |
| **D-114** | Login reads `app_user` through a narrow **`SECURITY DEFINER` function**, `auth_lookup(email)`. | 3 | `app_user` is admin-only and a user signing in has no role yet, so the lookup returned zero rows and every password looked wrong — including correct ones. The fix is a single controlled doorway returning only what authentication needs, NOT opening a table that holds password hashes to every query `app_role` can make. |
| **D-115** | Ingestion **normalises the customer name** before staging, and a lossy encoding repair raises a WARNING. | 1 | Found by looking at the committed data: `à¤®à¥‹à¤¹à¤¨` reached the customer table verbatim. The repair function existed and simply was not called — the most ordinary way a correct function fails to help anyone. The row is now flagged `ENCODING_PARTIAL` so an admin checks the spelling, because some characters are destroyed before the file reaches us. |
| **D-116** | `seed:dev` enrols a **known TOTP secret** for ADMIN and OWNER. Production enrolment is still to build. | 2 | Mandatory 2FA (D-52) is correct but means a fresh install has nobody who can sign in. This lives in `seed:dev`, which is refused against anything but a marked local database. **A real enrolment flow — scan a QR, confirm a code — remains outstanding for production.** |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-117** | **`packages/shared` is split**: the barrel is browser-safe, and anything importing `node:*` lives behind `@razorveda/shared/server`. | 1 | Re-exporting `fingerprint.ts` dragged `node:crypto` into the web bundle and webpack refused it outright. A package imported by both apps has to be honest about which half is which — otherwise the browser build breaks the first time a server-only helper joins the barrel. Only `fingerprint.ts` needed moving. |
| **D-118** | The web build tells webpack that `./x.js` may resolve to `x.ts` (`resolve.extensionAlias`). | 1 | The shared package writes ESM-correct imports — `./enums.js` for a file that is `enums.ts` on disk. Node and tsx resolve that; webpack does not. Dropping the extensions would break Node ESM, so the bundler is taught the mapping rather than the source being made wrong for it. |
| **D-119** | `apps/api` typechecks under **bundler resolution**. | 1 | Node10 resolution predates `exports` maps, so it could not see `@razorveda/shared/server`. `tsc` only typechecks here — `tsx` runs the code and honours `exports` directly — so this changes nothing at runtime. |
| **D-120** | Sections that do not exist yet are shown in the rail as **disabled labels under "NOT BUILT YET"**, not hidden. | 2 | An admin who clicks Reports and gets a 404 learns the product is broken. A greyed label says the same thing honestly and sets the expectation. |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-121** | The controller **converts `pg` Date objects to ISO strings** at the boundary; domain functions stay pure and string-typed. | 1 | `pg` returns `timestamptz` and `date` as JS Date objects. The worklist ordering is pure, typed on strings, and passed 20 unit tests — then the first real query crashed with `iso.slice is not a function`. The unit tests could not have caught it: the gap was never in the logic, it was in an adapter that did not exist yet. Converting here beats teaching every domain function about a database driver. |
| **D-122** | **Copy is logged BEFORE the number reaches the clipboard.** If the log write fails, the copy does not happen. | 2 | An unlogged copy is precisely what the control exists to prevent. Copying first and logging after would make the log best-effort, and the copy-velocity lock reads that log. The UI says "every copy is recorded with your name, the time and your IP" rather than implying a protection the system does not provide (docs/05, "the honest starting position"). |
| **D-123** | Each role **lands on its own screen** after login, and an admin who reaches an employee route is redirected rather than shown an empty one. | 2 | An employee sent to the Upload Centre sees "This section is for admins" and reads it as being locked out of the product. An admin shown an empty worklist reads it as a bug. |

| ID | Decision | Tier | Rationale |
|---|---|---|---|
| **D-124** | **An order is always recordable; only the CREDIT waits.** When a base price is unconfirmed, the order books, no ledger row is written, and the rep is told her credit is pending. | 3 | D-81 refuses to compute money from a guess, which is right for the ledger — but a rep on a call must be able to record a real sale. An order that happened, happened. Blocking her would make an accounting rule into a selling obstacle, and she would work around it. `creditKnown: false` separates the two: the sale is captured now, the credit follows when an admin confirms the price in Master Data. |
| **D-125** | `order_status_event` **WITH CHECK mirrors USING** instead of being admin-only. | 3 | It was `is_admin()`, so a rep booking an order could not write its own opening PENDING event — the order insert succeeded and the very next statement was refused. A rep can still only write events for orders SHE booked; she cannot touch anyone else's. |
| **D-126** | `attribution_ledger` INSERT allows `employee_id = current_employee_id()`, not admin only. | 3 | A rep booking an order legitimately creates its provisional BOOKED_CREDIT. Crediting anyone ELSE remains impossible, which is the part that matters — nobody can quietly move credit onto another rep's ledger. |
| **D-127** | The live preview and the real booking share **one code path**. | 1 | A preview computed a second way would eventually disagree with the payslip, which is precisely the trust problem this system replaces. What she sees while selling is what the ledger records. |
| **D-128** | **Two-factor enrolment is self-service, but only after a correct password**, and only once. | 2 | `evaluateLogin` refuses an admin with no enrolled secret (D-52), so a fresh deployment had nobody who could sign in — only `seed:dev` planted a secret, which is useless in production. Enrolment starts only after a password check (otherwise an email alone binds an authenticator and takes the account), the proposed secret travels in a signed 10-minute token (so it cannot be swapped between the two steps), and a second enrolment is refused. Losing a phone is an admin-assisted reset, never self-service: self-service re-binding would make a stolen password enough to take an account permanently. |
| **D-129** | **Writes to `app_user` from a pre-session context go through SECURITY DEFINER doorways** (`auth_enrol_totp`, `auth_touch_last_login`), never a direct UPDATE. | 2 | RLS refuses a WRITE the same way it refuses a read — by matching zero rows, silently. `UPDATE app_user SET totp_secret = ...` from a context with no role succeeded, changed nothing, raised nothing, and the caller read "zero rows" as its own business rule: *this account already has an authenticator*. A permissions failure wearing a business-rule message, with a clean log. The alternative — granting `app_role` write access to `app_user` — opens the table holding every password hash. One narrow function per operation is the smaller surface. The one-time enrolment guard lives inside the function, so two concurrent enrolments cannot both win. |
| **D-130** | The silent-write-filter trap gets its **own live-database test file**, asserting the hazard itself. | 1 | This is the fourth instance of one bug shape (audit_log, order_status_event, attribution_ledger, app_user) and unit tests cannot see any of them — the behaviour lives entirely in Postgres. `silent-write-filter.test.ts` asserts that a direct pre-session UPDATE reports success and changes nothing, that INSERT by contrast raises loudly (the asymmetry is *why* it went unnoticed), and that each doorway function works. Standing licence. |
| **D-131** | Ingestion staging uses the **certified `proposeMappingFromAliases`**, not its own hardcoded header lookups. | 2 | The controller reached for columns itself (`at(row,'Name') ?? at(row,'Customer name')`). `column-mapping.ts` — with the B8 deny-list and eleven tests — existed the whole time and was called by nothing. Two mapping implementations, and the tested one was dead. The live one never read `Order id`, `Final amount` or the parsed payment mode. |
| **D-132** | `paymentMode`, `externalRef`, `legacyCreditValue` and `city` are **staged into `normalised_json`**; `validate-row` now returns the parsed mode alongside the split. | 2 | Each was parsed correctly and then dropped at the staging seam, where commit read a key nobody wrote and silently took its fallback. Every ingested order committed as `payment_mode = UNKNOWN`, making Prepaid Ratio unmeasurable for the second time by a different mechanism (F5). `legacy_credit_value` was always null, so the reconciliation report docs/06 §4 promises had nothing to reconcile. |
| **D-133** | The ingested-order fallback number is keyed on **(customer, date, value)**, not (batch, customer). | 2 | The old fallback was unique per CUSTOMER per batch, so a repeat buyer's second order in one file hit `ON CONFLICT (order_number) DO NOTHING` and was dropped with no error and no exception shown — losing exactly the rows repeat-purchase analysis depends on. The new key is docs/06 §Idempotency's natural key, so a genuine re-upload still collapses while two different orders no longer collide. |
| **D-134** | `normalised_json` gets a **contract test** asserting every key commit reads is one staging writes. | 1 | The seam is an object literal on one side and a string index on the other; TypeScript sees `Record<string, unknown>` and is happy either way. A reader with no writer is dead code, a writer with no reader is a dropped field, and this fails on both. It found a fifth dropped field (`city`) immediately. Standing licence. |
| **D-135** | Staging runs the **real `resolveIdentity`**, with candidates from primary phone, **`customer_identifier`**, and pg_trgm name+pincode. | 2 | `resolved_action` was the literal `'CREATE'` for every row and `resolveIdentity` — defined and tested — was called by nothing. Dedupe half-worked by accident: commit ends in `ON CONFLICT (customer.primary_phone)`, so rows whose PRIMARY phone already existed collapsed on a unique index. That misses the point of rule 4 — the mobile is the business key and phones live in `customer_identifier`, many to one — so a customer arriving on her ALT number became a second customer. Uncertain merges (0.80–0.95) and unkeyable rows are now held for an admin instead of being committed as new customers. |
| **D-136** | `customer.ingestion_batch_id` records which batch created a customer. | 2 | Rollback could not identify the customers a batch created, let alone report them. Additive and weakens nothing: it makes an existing silent outcome visible. |
| **D-137** | Exit criterion 7 restated: **"no state from the batch is live"**, not "all derived state restored". | 3 → resolved by rule 2 | The original wording is unsatisfiable against a non-negotiable. `order_status_event` and `attribution_ledger` are append-only, so a rollback must be a compensating write. Customers go further: one created by an import may since have been called or sold to, and deleting that is worse than leaving a row with no live lead or order. Rollback therefore neutralises and REPORTS what it keeps. The criterion now describes what the system can actually guarantee, which is the testable claim. |
| **D-138** | Commit reports `customersCreated` / `customersMatched` / `rowsHeldForReview` from the **resolver's decision**. | 1 | It tested `resolved_action === 'CREATE'`, a value nothing ever wrote after D-135 and a value every row carried before it — so the admin was told a ten-row file created ten customers when it had created three. |
| **D-139** | Criterion 3's "ledger nets to zero" means the **realised** ledger (`is_realised = true`). The provisional `BOOKED_CREDIT` row survives an RTO. | 2 | Booked Value is status-independent by definition (docs/03 §2) and rule 3 says nothing is ever paid on it, so erasing the booking to make every row sum to zero would corrupt the booked figure to satisfy a wording. Realised nets to exactly 0.00, which is the only basis that pays. A test asserts the distinction explicitly, including the shape of the mistake — an incentive query that forgets `WHERE is_realised` pays a rep in full for a parcel that came back. |
| **D-140** | `StatusService` is the **only** way an order's status changes, and it is what realises credit. | 2 | `status-machine.ts` shipped in Phase 1 with a transition graph, a clawback rule and twenty tests, and nothing ever called it — the fourth certified-but-dead module found in this codebase. Until now the only writer to `order_status_event` was ORDER_ENTRY stamping the opening PENDING, so no order could reach DELIVERED and no rep could ever be paid. Realisation copies the BOOKED_CREDIT values rather than recomputing them: re-deriving at delivery would create a second source of truth for a number already agreed, and the two would drift the first time a base price was corrected. |
| **D-141** | A clawback is the **exact negation** of the realised entry, and belongs to the period it happens in. | 2 | Negating rather than recomputing means no rounding can separate a reversal from the credit it cancels. Cash basis (metric dictionary §6): reopening a closed month to reverse a payment already made would make a March report change in December, which is what the append-only ledger exists to prevent. |
| **D-142** | The repeat-purchase due date uses **MIN** of the order's `usage_days`, and a NULL never becomes a guess. | 2 | If a customer buys a 30-day cream and a 60-day supplement, the first item to run out creates the reorder conversation — waiting for the longest-lasting one misses the cream entirely, and she can be sold both on that call. `usage_days` is NULL for SKUs the client has not characterised (O-03); those customers simply do not enter the repeat queue. A missed opportunity is recoverable; a rep calling at a fabricated moment costs credibility. |
| **D-143** | The repeat lead is assigned to `customer.owner_employee_id` with `method = 'SYSTEM'` and an explicit `lead_assignment` row. | 3 — **needs confirmation, see gate** | Phase 3 deliverable 5 says the customer "enters the owning rep's worklist", but rule 6 says no auto-assignment. The reconciliation: rule 6 forbids an *allocation algorithm* distributing new leads from the pool; this assignment is determined by a recorded historical fact — who sold to and delivered for this customer — and `SYSTEM` already exists in `assign_method` for exactly this kind of automatic movement (it is how RECALL works). Every such assignment writes to the append-only ledger with a reason, so an admin asking "why is this on Nikita's list?" gets the same answer as for a manual assignment. |
| **D-144** | The repeat job runs under an **admin RLS context**, not a raw pool connection. | 1 | The first version opened a raw connection and carried a comment claiming it ran as the migrator role and bypassed RLS. It did not — the API pool connects as `razorveda_app`, so every query ran under RLS with no `app.user_id`, matched nothing, and the job reported `leadsCreated: 0` with `ok: true` having scanned an empty view of the customer table. Sixth instance of RLS refusing silently. A comment asserting the opposite is worse than no comment: it stops the next person checking. |
| **D-145** | The 48h untouched alert is a **query, not a stored event**. No notification table is added. | 1 | An untouched lead is fully derivable from `assigned_at` and the `activity` rows that already exist. Storing an "alert fired" row would create a second source of truth that can disagree with the leads themselves, and would need its own cleanup the moment the rep finally calls. Phase 3 owns DETECTION; Phase 4 owns notification delivery, and `findUntouched` is what a Phase 4 digest or an admin dashboard reads. |
| **D-146** | "Untouched" means **no activity since `assigned_at`**, not no activity ever. | 2 | A lead transferred between reps restarts its clock. Measuring from the lead's whole history would let the previous owner's calls buy the new one three free days on a customer nobody has spoken to since the handover. The predicate is written once and shared by the alert and the recall — if they drifted, the 48h warning would name a different set of leads than the 72h recall took. |
| **D-147** | The alert and recall are **ADMIN-only**, and the recall runs in the admin's RLS context. | 1 | A rep able to trigger the recall could clear her own list of the leads she had not worked, which is precisely backwards. Running inside the policy rather than around it means an admin sees every rep's leads because `is_admin()` says so. |
| **D-148** | The untouched predicate is **parameterised by table alias**, not string-replaced into shape. | 1 | The first version did `.replace(/\bl\./g, 'l2.')` on the shared SQL fragment. It works until a column or bound value contains the letters it matches on — a silent mis-scope in the predicate that decides whose leads are taken away. |
| **D-149** | The four incentive modifiers get a **table** (`incentive_modifier`), not constants. | 1 | docs/03 §6: "all slabs and modifiers live in tables, versioned, admin-editable. Never hardcoded." Only `incentive_slab` existed; the delivery-quality multiplier, prepaid bonus, product SPIF and repeat bonus had nowhere to live. One table because they share a shape — a condition, a value, an effective window. |
| **D-150** | Every incentive row carries `is_provisional`, and a computed figure is **provisional if any row it used was**. | 2 | O-09 is unanswered and docs/03 §6 marks its values as PROPOSALS. The flag travels with the data rather than being a note in a doc, so a statement cannot be rendered as final by a UI that forgot. `computeIncentive` can never clear it. An incentive figure that looks authoritative and is not gets paid, and the correction is a conversation about money already promised. |
| **D-151** | Order of operations: `payable = base × (slab% + prepaid% + SPIF%) × delivery-multiplier + repeat bonus`. | 3 — **needs confirmation, see gate** | docs/03 §6 lists the levers but not how they compose. The three percentages are all "% of realised credited value" so they combine additively; the delivery-quality lever is called a multiplier and prices in margin destruction across the whole payable, so it multiplies; the repeat bonus is flat rupees per order and cannot be scaled by a percentage without becoming something else. Defensible, explicit, and testable — but it is a money rule and it is mine, not the client's. |
| **D-152** | A period whose clawbacks exceed its realisations pays **zero, and does not carry forward**. | 2 | Cash basis (metric dictionary §7). Carrying the shortfall would mean a rep works a clean month and is paid nothing for a previous month's returns, and would make a closed period's figure change after it had been paid. |
| **D-153** | A realised value that **no slab covers is refused**, not paid at 0%. | 1 | A missing slab is a configuration gap. Paying zero silently looks like a calculation and would be believed; the error names the gap and says to add the slab in Master Data. |
| **D-154** | `scaleMoney` — an amount times a chain of decimal multipliers, **rounded once at the end**. | 1 | `percentOfMoney` rounds to paise per call, which is right for one step and wrong for a chain. Real divergences exist and are asserted in the tests: `333.33 × 3.33% × 1.15` is 12.76 rounded once and 12.77 rounded twice. A rupee per rep per month is small, and it is exactly the discrepancy that destroys trust in a payslip, because the rep can do the sum herself and get a different answer. |
| **D-155** | A **live test asserts every table has RLS enabled, FORCED, and carrying a policy**, with a justified allowlist. | 1 | `incentive_modifier` shipped with RLS off and no policy, and nothing failed — migration, seed and the whole suite passed while the table holding the incentive scheme was readable by any authenticated session. Found by auditing `pg_class` by hand, which is not a plan. `customer_identifier` was the same shape earlier (§7b item 3). schema.sql and rls-policies.sql are separate files and adding a table to one does not remind anyone about the other. Standing licence. |
| **D-156** | EES pipeline order: **shrink → neutralise → percentile-rank → weight**, and weights are read from the metric registry. | 2 | Shrinkage and neutralisation must precede ranking. Rank first and the small-sample rep has already topped the table; shrinking afterwards moves her score but not her position. Weights live in `packages/metrics` (mirroring docs/03 §5 under the parity test) rather than as constants in the scorer — a second copy would be a third place for them to disagree. |
| **D-157** | **docs/03 §5's stated goal for Bayesian shrinkage is not achieved by k=30, and no value of k achieves it.** Implemented as documented; the gap is recorded, not silently patched. | 3 — **needs a decision, see gate** | The doc says "a rep with 12 leads and one lucky order must not top the table" and prescribes k=30. Modelled properly the spike is in value-per-lead: 12 leads at ₹4,166 against 300 at ₹1,200, team mean ₹1,160. k=30 → 2019 vs 1196; k=2000 → 1178 vs 1165. Lucky leads at every k, because shrinkage moves both reps toward the same mean and her excess (12 × 3,006) stays proportionally larger than the steady rep's (300 × 40). **Shrinkage cannot reorder two reps on the same side of the mean** — it only compresses the gap. Fixing this needs a second mechanism (a minimum-lead threshold before ranking, or capping outliers), which is a scoring-definition change and therefore the client's call. An executable proof is in the tests. |
| **D-158** | `employee_score_daily`'s metric-named columns hold **observed** values; only `efficiency_score` holds a scored one. | 1 | The first version stored the shrunk, source-neutralised figures in `conversion_pct` and `rto_pct`. A rep with three deliveries and ZERO returns was recorded at `rto_pct` 0.2182, because shrinkage had pulled her toward a team mean containing someone else's return. Correct as a ranking input, false as a fact — and these columns are named after metrics with dictionary definitions, so any report reading them would have published it. Observed rates are the ratio of SUMS, never the mean of per-cohort ratios. |
| **D-159** | The EES cohort query collapses `attribution_ledger` to **one row per order** before joining. | 1 | Joining the ledger directly fanned the result out: every order carries a BOOKED_CREDIT and a REALISED_CREDIT, and a returned one also a CLAWBACK, so the delivered count counted order-times-ledger-row. A rep with three delivered orders scored as six; one delivered plus one returned came out as delivered 2, RTO 3. Nothing failed — the run succeeded, wrote rows and was idempotent. The only symptom was a conversion rate of 2.0, and a rate above 1 was the sole reason anyone looked. A live-database test now recomputes the counts straight from `order` with no ledger join and asserts they match. |
| **D-160** | `employee_score_daily` gets a **write policy**; the RLS coverage test now checks per COMMAND, not per table. | 1 | The table shipped with a SELECT policy and no write policy at all, so nothing in the system could ever have written a score. Fifth instance of a read policy without its write half (audit_log, order_status_event, attribution_ledger, app_user were the others). The coverage guard added one commit earlier missed it because it asked only "is there A policy?". It now asserts every table can be both read and written. INSERT raises rather than filtering silently, so this one at least announced itself. |
| **D-161** | Certified views derive order state from **`order_status_event`**, never `order.current_status`. | 1 | Exit criterion 2 requires the August close pack to produce identical numbers when re-run in December. `current_status` is a mutable snapshot: an order delivered on 12 August and returned on 3 October reads RTO today, so a naive August report would show it as an August return — a month in which it was delivered. August would change every time a later parcel moved. Proven: after injecting a return dated 60 days out, the re-run is byte-identical, while a query over `current_status` diverges (3 vs 5 delivered). |
| **D-162** | The certified views live in **`packages/metrics/sql/views.sql`** and are applied by `db:migrate` after the schema. | 1 | docs/04: reports read certified views and nothing else. Keeping the SQL beside the registry that documents it makes the pair obvious, and applying it in migrate means a fresh database is reportable rather than needing a remembered extra step. |
| **D-163** | **Materialised views are revoked from `app_role` and exposed only through `security_barrier` views.** | 2 | Postgres allows an RLS policy only on a TABLE. A matview carries none, and `ALTER DEFAULT PRIVILEGES` (D-27) granted `app_role` SELECT on all six the moment they were created — so any signed-in rep could read every colleague's daily KPIs, the full RTO analysis, and every customer phone number in `mv_repeat_due_queue`. Nothing failed: the isolation tests passed and the policy-coverage tests passed, because both filter `relkind = 'r'` and never saw them. Each matview is now reached through a `v_` wrapper carrying the predicate the matview cannot, and the coverage test asserts no matview is directly readable. |
| **D-164** | Exit criterion 3 is enforced by asserting **every column of every certified view maps to a registry key**, with explicit dimension and alias lists. | 1 | "No report may display a metric absent from docs/03." Reports read only the views, so the views are where it is enforceable. A column in a matview WILL reach a screen — that is what it is for — and the answer to "what does this mean?" has to be the dictionary rather than whoever wrote the SQL. Dimensions are listed by name rather than pattern-matched: a rule like "ends in `_id`" would silently excuse a real measure. |
| **D-165** | `order.lead_id` is required for an order to appear in scoring or the source funnel. | 2 | Both join `lead` to reach the source. An order with a NULL `lead_id` is invisible to EES and to `mv_source_funnel_daily` — it still counts in money and in the order register, so nothing is lost financially, but the rep earns no scored credit for it. Ingestion and Order Entry both set it; a manually inserted order may not. Recorded because it is a silent omission rather than an error. |
| **D-166** | Reports read the `v_` wrappers and never recompute a ratio. | 1 | docs/04: "no report computes its own arithmetic". Every query in `reports.service.ts` is a filter and a SUM. A wrong number is then wrong in `views.sql`, in one place, for every report at once — rather than in five reports that each drifted separately, which is the failure the client is paying to escape. |
| **D-167** | XLSX export uses **exceljs**, and money reaches the sheet as a NUMBER. | 2 | The spec says XLSX, so CSV is not a substitute. Money and counts come out of Postgres as strings deliberately — no float touches a money value — but writing those strings into cells means every total a human types in Excel silently returns zero, which is exactly the "spreadsheet you cannot trust" this system replaces. Converted at the cell boundary, once. |
| **D-168** | The export is refused **inside the service**, not only by the route guard. | 1 | CLAUDE.md §7 puts employee export on the do-not-build list, and rule 8's whole protection model is that there is no button. A guard on a controller is one refactor away from being forgotten; the service checks the role itself, so a new route cannot accidentally grant it. |
| **D-169** | The audit row is written **before** the file is built, and its `log_id` IS the export id. | 1 | A log that only records completed exports cannot answer "what was attempted and failed?" — which is the shape of an exfiltration attempt that timed out on a large range. Reusing the audit primary key avoids a second identifier for one event, which could drift from it. |
| **D-170** | **Dependency-audit state recorded, not silently accepted.** `npm audit` reports 15 issues; `npm audit fix` made it 18 by pulling in `sharp`, and was reverted. | 2 | The critical (vitest) and two highs (vite) are dev-only. One high is `multer` via `@nestjs/platform-express`, which IS a production path and handles uploads. exceljs contributed only moderates (`file-type`, `qs`). Not fixed in this pass because a dependency-hygiene sweep deserves its own focused change rather than being bundled into a feature commit — but it is a real finding and it belongs to someone. |
| **D-171** | The RTO-adjusted target (F11) ships as a **read-only comparison report**. Nothing is switched on. | 2 | It changes what seven people are measured against, so the client sees the movement before it moves. The report shows the flat ×1.15 the sheet uses today beside the corrected figure and the percentage gap. On the current data: a rep at 25.9% RTO is asked for **+17.4%**, one at 8% for **−5.5%**, and one at exactly 13.0% for the same as before — because 1.15 is `1/(1−x)` at x = 13.04%, so the flat rule was correct for precisely one RTO rate and wrong for every other. |
| **D-172** | Required Booking is **capped at 60% RTO**, and the cap is reported. | 2 | `1/(1−0.95)` is a twentyfold target. That is not a target, it is a signal that something is badly wrong, and printing it as a number buries the signal. The cap is named in the report's notes with the words "a rep at this rate needs a delivery conversation, not a bigger number". |
| **D-173** | A rep with **no shipped orders has an UNKNOWN RTO, not a zero one**; the team rate is used and flagged. | 2 | Found by reading the comparison rows rather than the totals: every rep with no orders showed 0.0% RTO — indistinguishable from a flawless record — and the corrected rule handed each a **13% target cut** for having no evidence at all. The team rate is the fail-safe: it neither rewards an absence of returns she never had the chance to incur, nor punishes her for the team's. A genuine 0% over real volume is kept as her own. |
| **D-174** | The comparison reports the **effective** rolling RTO, not the raw query value. | 1 | Printing a raw 0.0% beside a +3.5% uplift reads as a bug to anyone checking. The reader needs the number the arithmetic actually used, plus the flag saying where it came from. |
| **D-175** | Assignment Quality ranks sources by **yield per lead**, not conversion count. | 2 | A rep who converts fewer META_ADS leads at triple the value is better at META_ADS; a count says the opposite and would push an admin to hand her the wrong work. This is the one report whose entire purpose is to be acted on — D-02 removed the allocation engine, so assignment stays human, and this is what makes it informed rather than habitual. |
| **D-176** | The fan-out guard's probe orders are **unique per run**. | 1 | It used fixed order numbers, cancelled them in `afterAll`, and then `ON CONFLICT DO NOTHING` silently skipped recreating them — so it passed once and afterwards asserted over an empty set. A guard that stops guarding after its first run is worse than no guard. It also now mirrors EES's own scope (`lead_id IS NOT NULL`, D-165) so it fails for fan-out and not for a documented gap. |
| **D-177** | The month-close pack is assembled in **one call**, not section by section. | 1 | Nine separate requests could straddle a matview refresh and disagree with each other — the "two reports, two answers" problem the whole system exists to end. Built in 174 ms, and identical on re-request. |
| **D-178** | The pack carries a **credit reconciliation** section naming the gap between realised VALUE and realised CREDIT per rep. | 2 | Section 1 reports the full order value delivered; section 2 reports the incentive base from the ledger. A rep who delivered ₹153,000 can legitimately have an incentive base of ₹3,000, and a reader seeing those side by side with no explanation concludes the pack is broken. Three correct reasons they differ: `company_base_value` is subtracted (F7), historically imported orders carry no ledger rows, and an order booked against an unconfirmed base price has no credit until an admin confirms the price (D-124). The gap is now computed and named rather than left to be noticed. |
| **D-179** | Source P&L **omits ROI** rather than computing it from a zero. | 1 | docs/04 says "ROI where spend entered". No campaign spend is recorded anywhere in this system, so dividing by it would produce infinity and be believed. The section says why it is absent and what would make it appear. |
| **D-180** | Product Line P&L lists **all seven lines, including those that sold nothing**. | 1 | A line missing from a P&L reads as "no data"; a line showing zero reads as "no sales", and only one of those is a finding. F8 was a single product column hiding exactly this. |
| **D-181** | One rep's configuration gap **does not fail the whole pack**. | 1 | `computeIncentive` refuses when no slab covers a rep's realised credit (D-153), which is right for that rep's statement and wrong as a reason to produce no month-end pack at all. The section reports the gap in place of that rep's figures. |
| **D-182** | Scheduled deliveries get a **table** (`notification_outbox`), unlike the 48h untouched alert which is a query (D-145). | 2 | The difference is that a DELIVERY is not derivable from anything: "was the 07:30 plan sent to Nikita on Tuesday?" has no answer in the lead or order tables, and exit criterion 5 requires five consecutive days of sends to be VERIFIED. It also provides idempotency — `ux_outbox_slot` is unique on (kind, slot_key, recipient), so a scheduler that retries after a crash cannot send a rep two morning plans, and the guarantee does not depend on the caller remembering to check. |
| **D-183** | The transport seam is built; **no provider is**. Unconfigured EMAIL/WHATSAPP **refuses loudly** rather than falling back to a file. | 2 | Credentials are a decision nobody has made. `FileTransport` writes each message to disk exactly as it would be sent, so the digests can be read and corrected now and the scheduler can be exercised end to end. But a message recorded as EMAIL that was written to a directory is worse than one that visibly did not send — so the FILE channel is recorded as FILE, and `/digests/sent` warns in words when every delivery in a range used it: *"the messages were written to disk and reached nobody. Criterion 5 is not satisfied by these."* |
| **D-184** | The outbox row is **inserted before the transport is touched**. | 1 | If the slot is taken the database refuses and the send never happens. Sending first and recording after would make a crash between the two produce a duplicate on the next run — the one failure an append-only-ish delivery log cannot clean up. |
| **D-185** | The zero-dials alert returns **nothing before 14:00**, by design. | 1 | docs/04 asks for it *at* 14:00. Firing at 09:00 would flag every rep who has not started yet and train admins to ignore it, which costs more than not having it. It is the only alert in the set that arrives while the day can still be saved. |
| **D-186** | The Phase 5 security review is an **adversarial suite against the running API**, not a unit test of the policies. | 1 | The policies have been tested since Phase 1 and passed while five separate write paths were silently broken. What matters is whether the HTTP surface leaks, because that is the surface a rep actually has. 28 attacks: IDOR on every id-taking endpoint, cross-rep booking and activity, pagination abuse, every admin route as a rep, unauthenticated access, and a forged session cookie. |
| **D-187** | **Finding (LOW): `POST /activity` returned HTTP 201 Created for every refusal**, including "that lead was not found". Fixed. | 2 | The security behaviour was correct — nothing was written and nothing leaked, RLS held. But a client trusting the status code would record a call that does not exist. The web UI happened to check the body's `ok` field as well, so no user ever saw it; a second client would not have been so lucky. Now 404 when the lead is not visible (same answer as `GET /leads/:id`, deliberately indistinguishable from a lead that does not exist) and 400 for genuine field validation. `ApiError` carries `field` so fixing the status code did not silently cost the form its field highlighting. |
| **D-188** | Refusals answer **404, never 403**, for records another rep owns. | 1 | A "forbidden" confirms the record exists, which tells an attacker which ids are real and is enough to enumerate the customer base. Asserted explicitly rather than left to convention. |
| **D-189** | **The copy-velocity lock was installed, unit-tested, and INERT.** Reading `pii_access_log` needs a SECURITY DEFINER doorway. | 1 | `evaluateVelocity` was correct and thirteen tests proved it. The lock never fired: `pii_access_log` is read-admin-only by design, the check runs AS THE REP, the query returned zero rows, and the evaluator correctly concluded there was nothing to see. Nothing errored. Eighth instance of RLS returning nothing and the caller reading it as a fact about the business rather than about its own permissions — and the sharpest, because the comment above that very policy warned an admin-only INSERT "would have silently disabled the copy-velocity lock". Right about the mechanism, wrong about which half. `security_recent_pii_copies` is scoped to the caller, so the doorway cannot be turned into a way to read a colleague's copy history. |
| **D-190** | The lock, the session revocation and the admin alert happen in **one SECURITY DEFINER function**. | 2 | The caller is the rep being locked — an EMPLOYEE who cannot write `app_user` or `notification_outbox`, and must not be able to. Doing all three together means they cannot half-happen: an account locked with live sessions still working, or locked with nobody told, are each worse than not locking. Revoking sessions is the half that actually stops her; without it the lock takes effect at her next sign-in, which may be tomorrow. |
| **D-191** | Only **COPY** counts toward the lock, never VIEW. | 1 | A VIEW is a rep looking at the lead she is about to call — the normal act the whole system is built around. Counting it would lock a rep for working quickly. A COPY is the number leaving the application, which is the event with exfiltration value. |
| **D-192** | The rep is **told she has been locked**, and the admin alert leads with the innocent explanations. | 2 | A mysterious sign-out invites a rep to assume she has been sacked by a computer; the message says an admin can unlock it and invites her to explain. The alert tells the admin to look at the copy history *before speaking to her* and names the innocent causes — a stuck key, a browser extension, a batch of numbers copied before a call block. An alert that reads as an accusation gets someone in trouble for a keyboard fault. |
| **D-193** | Already-locked accounts do **not** re-alert. | 1 | Otherwise an admin gets one alert per copy for an account that is already stopped, and learns to ignore the alert that matters. |
| **D-194** | The restore drill verifies **security, not just rows**. | 1 | A restore that loses row-level security is far worse than a failed restore: the application starts, every query succeeds, every rep sees every customer, and nothing says so. So the drill asserts policies, FORCE flags, SECURITY DEFINER doorways and append-only triggers — and then counts the same table as the owner and as a rep, because policies EXISTING is not policies WORKING (D-49). |
| **D-195** | **`--no-privileges` must not appear in the dump or the restore.** | 1 | Found by the drill: the isolation check died on "permission denied for table lead". GRANTs are how `app_role` reaches a table at all; RLS decides which ROWS it sees. Restoring policies without grants produces a database with every policy present and an application that can read nothing — fail-closed, which is the right direction and still a broken restore. Removing the flag from `pg_dump` alone achieved nothing: `pg_restore` was still told to skip them, so the grants were in the archive and discarded on the way in. Both sides have to agree. |
| **D-196** | **`REFRESH MATERIALIZED VIEW` is part of the restore procedure**, not a tidy-up. | 1 | `pg_dump` stores a matview's definition, never its contents, so a restored database has six EMPTY ones. Every report reads them, and an empty matview returns zero rows rather than failing — the morning after a restore every report would show a quiet month and nothing would say the data had not come back. |
| **D-197** | Roles are **not in the dump**, and the README says so. | 1 | `pg_dump` is per-database; `app_role` and `razorveda_app` are cluster-level. A restore onto a fresh machine needs `pg_dumpall --roles-only` first or it fails on every `GRANT ... TO app_role`. The drill restores into the same cluster, so it passes on a technicality — and says so rather than letting that stand as proof. |
| **D-198** | Version skew is reported as **benign but named**. | 1 | `pg_dump` 18 against a PostgreSQL 16 server emits `SET transaction_timeout = 0`, which 16 rejects. It affects no data. That is exactly the danger: it trains an operator to ignore a warning that might not be harmless next time. Use a matching-version `pg_dump` in production. |
| **D-199** | **The upload body limit was Express's 100 kB default, so ingestion could not accept one real day's file.** Fixed, scoped to that one route. | 1 | 2,000 rows/day at ~175 bytes is ~350 kB, and base64 adds a third. The effective cap was about 75 kB of CSV and the failure was a bare 413. It would have surfaced the first time the client uploaded a real export. The generous limit (25 MB) applies to `/ingestion/upload` alone; 256 kB everywhere else. A 25 MB body limit across the whole API would be a denial-of-service surface on every endpoint for the benefit of one. |
| **D-200** | Uploads are **validated at the door**, by content rather than by extension. | 2 | An XLSX is a ZIP; read as UTF-8 it became thousands of replacement characters and was rejected by the column-shift detector with a message about type contracts — correct, and useless to an admin who just wants to know why their file did not import. Magic bytes now name the format (.xlsx, .xls, PDF, gzip), a NUL byte in the first kilobyte catches Excel's UTF-16 "Unicode Text" export, and a first line with no delimiter is quoted back so the admin recognises the wrong export. |
| **D-201** | Security headers are **hand-written, not a dependency**. | 2 | Six headers is not enough code to justify another package in a tree already carrying fifteen audit findings it did not choose (D-170). The API's CSP is `default-src 'none'` because it serves JSON and one XLSX and should be able to do nothing else; the web app needs its own, looser policy. HSTS is production-only — sending it from a local http server teaches the developer's browser to refuse `http://localhost` for two years. |
| **D-202** | Rate limiting is **in-memory, and the limitation is stated rather than discovered**. | 2 | Counters live in the process. One API instance is the deployed shape (rule 9), so this is accurate today; the moment a second runs, each keeps its own count and the effective limit doubles. Redis is already a dependency for BullMQ and is where this belongs if the API is ever scaled out. The window slides rather than resetting, because a fixed window allows `max` at 11:59:59 and `max` again at 12:00:00. Buckets are swept hourly — without that the map grows forever under an attacker-controlled key. |
| **D-203** | CSRF has **two independent defences**, and the residual gap is stated. | 1 | `SameSite=strict` on both cookies is enforced by the browser before a request is sent. Origin checking is second, because SameSite is a browser promise and a non-browser client does not make it. A request with NO Origin header is allowed through — curl, the digest scheduler and the test suite all send none — so the gap is named rather than implied. |
| **D-204** | CI runs the live database and security suites, and **reports** the dependency audit without failing on it. | 2 | More than half of this project's defects lived in SQL and RLS and were invisible to unit tests, so `test:rls` — the isolation tests, the adversarial review, the coverage guard, the velocity lock, the fan-out guard — runs against a real Postgres in CI. The audit is `continue-on-error` deliberately: the tree carries 15 findings including a high in `multer` on the upload path, and failing the build today means a permanently red pipeline everyone learns to ignore. Turn it enforcing once the tree is clean; that is the whole remediation plan. |
| **D-205** | **CI was shipped broken and emailed the repository owner on every push.** Fixed by making it smaller, not cleverer. | 1 | Two causes, both mine. `@embedded-postgres/windows-x64` was a hard devDependency, so `npm ci` could not install on `ubuntu-latest` — it is a Windows convenience for running Postgres without Docker and is now an `optionalDependency`, which npm correctly skips on Linux. And `npm run typecheck` ran `tsc -b` against a root `tsconfig.json` that has never existed: the script had never worked, because every check in this project has been run per-project by hand. Both would have been caught by running the workflow once, which I could not do — so the fix is a pipeline I have verified end to end locally rather than one that claims more. |
| **D-206** | The live database suite and the restore drill are **deliberately absent from CI**, with what it would take written into the workflow. | 2 | They are the tests that have actually caught things — the isolation tests, the adversarial review, the coverage guard, the velocity lock, the fan-out guard. They belong in CI. They are out because the first attempt added them unverified and turned the pipeline red on every push. A green pipeline that runs less is worth more than a red one that claims more: a failing build everyone has learned to ignore protects nothing. |
| **D-207** | `concurrency` with `cancel-in-progress` on the workflow. | 1 | A burst of pushes produced a burst of failure emails. Superseded runs are now cancelled. |
| **D-208** | **There was no way to unlock an account.** Added, audited, and requiring a written reason. | 1 | Eight separate messages in the codebase told users "an admin can unlock it" — the login refusal, the TOTP enrolment refusal, the message the locked rep sees, the alert the admin receives. None of them was true. The velocity lock shipped able to put a rep out of the system permanently with raw SQL as the only way back. A control with no release is not a control, it is a trap. The reason is mandatory and the original lock reason is captured in `before_json` before the column is cleared, or why the account was ever locked would be lost. |
| **D-209** | Sessions are **revoked, never deleted** — including by the velocity lock. | 1 | The lock's first version deleted them, which worked only because SECURITY DEFINER runs as the owner: `app_role` has INSERT, SELECT and UPDATE on `app_session` and deliberately no DELETE. Deleting also destroyed where the rep was signed in from and for how long — exactly what an admin needs when deciding whether the lock was fair, and part of the access history offboarding is required to preserve. Offboarding hit the same wall as a plain "permission denied", which is how it was found. |
| **D-210** | Offboarding **never deletes the employee**; it removes access and reassigns work. | 2 | Every order she booked references her, every ledger entry credits her. Deleting would fail on the foreign keys or cascade and take a year of attribution with it. Leads return to the POOL rather than to a named rep (D-02 removed auto-assignment). Owned customers are released, because `owner_employee_id` drives the repeat queue and leaving it pointing at an exited rep switches off the highest-ROI automation for a slice of the customer base. Open orders are REPORTED, never reassigned: credit belongs to whoever made the sale (rule 3), and moving a pending order onto someone else's ledger pays the wrong person. |
| **D-211** | `RATE_LIMIT_DISABLED=1` exists for the live test suite, and the API says so at every boot. | 2 | The suite signs in as several people several times from one address and trips the login rule legitimately — the control working on the wrong target. Deliberately NOT tied to `NODE_ENV`: a developer running locally should have rate limiting on, because that is when its rough edges get found. It has to be asked for by name. |
| **D-212** | The console's session query used a column that does not exist (`expires_at`), and **returned nothing while reporting success**. | 1 | The real column is `revoked_at`. The endpoint answered `200` with an empty list, which reads as "nobody is signed in" — the single-session rule would have looked perfectly observed on a screen that could not see a session at all. Found by running it, not by typechecking it: the query is a string. |
| **D-213** | **The API was far ahead of the UI.** Reports and the security console now have screens. | 2 | Phase 4's promise is "nobody prepares a report, ever again" — and every report existed with no way to open one. Reading the month-close pack meant curl. A report an admin cannot open has not replaced a spreadsheet, and a runbook that says "run this command" is not a deliverable. The same was true of the security console, which mattered more: the velocity lock could put a rep out mid-shift and the only way back was an admin with database access. |
| **D-214** | Report tables render **generically from the columns the API returns**. | 2 | The certified views are the single definition of every number (rule 10). A hand-written column list in the UI would be a second place for them to drift — a report would silently stop showing a column the moment the view gained one. The one exception is the self-reported marker, which is derived from the column NAME (`*_self_reported`) so a new claimed metric is labelled without anyone remembering to. |
| **D-215** | The live test suite runs **sequentially**. | 1 | Its files share one database and one set of seeded reps, and several mutate global state — the velocity-lock test locks an account, offboarding exits an employee, the recall test unassigns leads. In parallel the lock fired while the adversarial suite was signing in as the same rep, which presented as a security test failing intermittently. A flaky security test is worse than a slow one: it gets re-run until it passes. |
| **D-216** | **Master Data exists, and O-09 is now answerable by the client rather than by me.** | 2 | The incentive scheme was the proposals from docs/03 §6, so every payable figure said "not approvable for payment" — and answering it required someone to send numbers to a developer. It should require an admin to type them in. Entering a scheme and marking it confirmed clears the warning; proven end to end, `provisional=false` and the payment caveat gone. |
| **D-217** | Twenty active SKUs had **no confirmed base price and no screen to confirm one**. | 1 | An order on such a SKU books normally and the rep earns NOTHING (D-124). The exception digest has been reporting this every morning with no route to fixing it short of raw SQL. Confirming records who and when, audits the previous value, and shows what a rep now earns at MRP. A base price above MRP is REFUSED rather than clamped — the rep's credit would be negative, which means it is a typo, and clamping would hide it. |
| **D-218** | Editing the incentive scheme **supersedes; it never overwrites**. | 1 | docs/03 §6: "versioned". Changing a slab closes the old row with `effective_to` and opens a new one, so recomputing March in December still finds March's scheme. Editing percentages in place would silently rewrite every incentive statement ever produced. Proven: 4 superseded rows retained, closed 2026-08-31, with the new scheme live from 2026-09-01. |
| **D-219** | A slab set with a **gap is refused at entry**, not at calculation. | 2 | `computeIncentive` refuses when no slab covers a value (D-153), which is right but arrives at month end. The gap is caught while an admin is looking at the numbers: *"a rep landing in it would have no slab at all, and her statement would refuse to calculate."* The lowest slab must start at 0 for the same reason. |
| **D-220** | Superseded future-dated slabs are **closed to an empty window, not deleted**. | 1 | `app_role` has no DELETE on `incentive_slab` — the same no-delete posture as everywhere else, and the right one. `effective_to = effective_from - 1` is a window no date falls inside, which is precisely what "entered but never in force" means, and it keeps the record that someone once entered it. Third time a missing DELETE grant has surfaced this way (app_session, incentive_slab), and each time the no-delete answer has been better than the grant. |
| **D-221** | **CRITICAL — a rep could pay herself.** She could book an order and walk it to DELIVERED in six requests, realising her own credit with no admin, no courier and no parcel. Fixed. | 1 | The most serious defect found in this project, and the first review missed it entirely. Every one of those 28 attacks asked *"can rep A reach rep B's data?"* — and RLS answered correctly every time. None asked whether a rep may do something illegitimate to her OWN data, which RLS cannot answer and was never meant to. **Isolation is not authority.** A rep may now record what she KNOWS (the customer confirmed; the customer cancelled) and never what the COURIER knows. Everything from PROCESSING onward is admin or ingestion. Cancelling stays open because it can only ever reduce what she earns, so there is no incentive to abuse it and blocking it would make her chase an admin to record a change of mind. |
| **D-222** | The order list returns the transitions **the caller may make**, not every legal one. | 2 | Computed server-side per session, so the screen cannot offer a button the API will refuse or hide one it would allow. A rep sees "confirm" and "cancel"; an admin sees the courier states. The alternative — the UI filtering a full list — would put the authority rule in two places, and the copy in the browser would be the one an attacker edits. |
| **D-223** | Orders & RTO exists as a screen, because **credit could not realise without one**. | 1 | Credit is earned on delivery (rule 3). Marking an order delivered existed only as an API call, so in practice every rep's realised value was zero and every incentive statement was empty. The money half of the system had no way to complete outside curl. |
| **D-224** | Customer 360 is **not admin-guarded**; RLS decides per row. | 2 | This is the screen a rep opens before she dials — what has this customer bought, what went wrong last time, when is she due. Locking it to admins would leave her calling blind, which is the situation the nine spreadsheets created. `customer_isolation` shows her only customers she has an assigned lead for or owns, so "not admin-guarded" does not mean open: the database decides per row rather than the route deciding for everyone. Verified: admin found 6, the rep found 2 on the same search. |
| **D-225** | **Opening a profile is a logged PII view.** | 1 | docs/05: "pii_access_log writes on every number view and copy." The profile shows a full number, so fetching it IS a view. Logged AFTER the read succeeds — logging first would record a view of a customer RLS then refused to show, turning the access log into a record of attempts rather than of what was actually seen. Views never count toward the velocity lock (D-191): a rep reading her own customers all morning is working. |
| **D-226** | Search matches **alt numbers, not just `primary_phone`**. | 1 | A customer who rings in from her second phone is the same person (rule 4). A search that looked only at `primary_phone` would say she does not exist, which is the F1 failure — 375 customers appearing in more than one tab — reproduced in a search box. Written as `EXISTS` rather than a join plus `DISTINCT`: the join multiplied a customer by her identifiers, `DISTINCT` papered over it, and `DISTINCT` then made the `ORDER BY` illegal because `lifetime_value` is selected cast to text. |
| **D-227** | Admin and rep runbooks written, and every instruction names a **real screen**. | 1 | They were deliberately left until after the UI existed. A runbook that says "run this curl command" is not something you hand a tele-sales team, and writing one earlier would have documented an intention rather than a system. Writing them found a real error of my own: I described the worklist as four bands when it has five, and put Repeat due in the wrong position — checked against `order-worklist.ts` rather than from memory, which is the only way a runbook stays true. |
| **D-228** | The runbooks say what the system **will not do, and why**. | 2 | "You cannot export anything" reads as distrust unless the reason is next to it. Each restriction is given its reason — no export because there is no button and therefore nothing to be talked into; no marking your own order delivered because that is the transition that pays you. A rule a person understands is one they do not try to work around. |
| **D-229** | Criterion 5 remains **unmet**, and the README says so. | 1 | It asks for runbooks *and sign-off from both groups*. The documents exist; nobody outside this repository has read them. Marking the criterion done because half of it is finished would be exactly the kind of claim this project keeps catching elsewhere. |
| **D-230** | **A volume generator, because every timing until now was taken on a few hundred rows.** | 1 | `npm run db:seed:volume -- --days 90` builds 90 days at the client's stated 2,000 orders/day: 180,000 orders, 996,000 status events, 1.75M rows, 347 MB. Shape rather than size — nine sources, a realistic 70/15 delivered/RTO mix, full append-only event chains, activity on 60% of leads. Deterministic seed, so two runs are comparable. It found four production-blocking defects in one sitting, none of which any test could have caught at fixture size. |
| **D-231** | **CRITICAL — the 50-row cap was never implemented.** A rep received all 14,381 of her open leads, names and full phone numbers, in one request. | 1 | `EMPLOYEE_MAX_PAGE_SIZE` has existed in `packages/shared` since Phase 0 and was imported by nothing. docs/05 test 4 requires the cap and the test for it PASSED — vacuously: it ran `SELECT lead_id FROM lead LIMIT 51` and asserted the result was under 50, which only proved the fixture was small. The copy-velocity lock stops four copies in ninety seconds; this handed over fourteen thousand numbers in a single GET, which made the lock decoration. Now capped in SQL, and the vacuous test is rewritten to assert what the database can honestly prove. |
| **D-232** | **The month-close pack never returned at volume.** 305 s timeout → 4.74 s. | 1 | Four separate causes, found by timing each section rather than the whole: a missing index on `attribution_ledger(order_id)` — the ledger is read by order on every delivery and had no index for it; a self-join over 996,000 events for dispatch TAT; a double `LEFT JOIN` that cross-multiplied every rep's leads by her customers; and `NOT EXISTS` over all 180,000 orders where driving from the events was the same answer. The pack now reports its own per-section timings, so "it is slow" is never again the finding — WHICH section is slow is. |
| **D-233** | **`column::date BETWEEN a AND b` is unindexable, and it was everywhere.** | 1 | Wrapping a column in a cast means no index on it can be used, so adding one changes nothing — which is the tell. `event_at::date`, `occurred_at::date` and `date_trunc('month', delivered_date)` each turned a hot query into a full scan of the largest tables in the database. Half-open ranges (`>= from AND < to + 1`) select exactly the same rows and read an index. |
| **D-234** | **An RLS policy is a query and needs its own indexes.** | 1 | `customer_isolation` asks "does this caller have a lead for this customer?" for every row it considers. Without `lead(customer_id, assigned_to)` that is a scan per row, and it alone accounted for half the worklist's response time. The most forgettable index in the schema, because the query that needs it is not written in any file a developer reads. |
| **D-235** | Timing an expensive query **as the table owner proves nothing about the application**. | 1 | The dispatch-TAT self-join measured 327 ms run as the migrator and 58 seconds run as `app_role` — the owner is not subject to the same policies, so the plan is not the same plan. Every benchmark in this pass that mattered was re-run inside an RLS context. |
| **D-236** | **A mutation check: break the protection, confirm the test notices.** | 1 | Two of this project's worst defects sat behind passing tests — the 50-row cap whose test asserted the fixture was small, and self-dealing, which twenty-eight adversarial tests missed because every one asked "can rep A reach rep B's data?". Adding more tests under the same conditions would have found neither. `node scripts/mutation-check.mjs` breaks six protections in turn and asserts the guarding test goes red. A mutation that survives is not a passing test; it is a test that was never testing anything. |
| **D-237** | **Two genuine gaps found and closed.** The incentive slab-gap rule was untestable, and the money-rounding test proved the primitive rather than its use. | 1 | Deleting the slab-gap check entirely broke nothing: the only way to reach it was through a service needing a database and an admin session, so it is now `slab-rules.ts` — a pure function with seven tests, including the off-by-one hole ("0–99,999 then 100,000+") that reads as contiguous and is how the client's own sheet is written. And replacing the single chained multiply in `computeIncentive` with two rounded steps broke nothing, because the worked example uses figures that round identically either way: `scaleMoney` was proven correct while nothing checked the engine USED it correctly. Both now caught. |
| **D-238** | The harness reports **INCONCLUSIVE, never SURVIVED**, when it cannot confirm the API restarted. | 1 | Its first version waited for `/health` to answer — and the OLD process was still holding the port, so it answered with unmutated code and every API-dependent mutation was reported as "this protection has no test", about tests that catch it perfectly well. A false *"you have no coverage"* is the more dangerous direction: it invites a redundant test, or distrust of a good one. Both API mutations were then verified by hand and are CAUGHT. |
| **D-239** | The harness warns that it may leave the **running API serving mutated code**. | 1 | It restores the source; `tsx` does not hot-reload, so a dev server started mid-run keeps the mutation until somebody restarts it. The next live suite then fails for a reason no longer present in the tree — which looks exactly like a real regression, and cost a confused minute before being understood. |
| **D-240** | **Dependency advisories are triaged by reachability, not by count.** 18 → 5, critical 1 → 0. | 1 | `npm audit fix --force` would have upgraded Next AND *downgraded* exceljs from 4.4.0 to 3.4.0 across a major version, breaking every Excel export — to fix a `uuid` bounds check in v3/v5/v6 that exceljs cannot reach, because its single call site is `uuidv4()` with no arguments. Reading the count alone leads there. So each advisory got a verdict with its evidence, written to `docs/10-dependency-risk.md`. NestJS 10 → 11 closed the one chain genuinely on a live path (express/qs/body-parser, which every request crosses); vitest 2 → 4 closed the critical and four more. |
| **D-241** | The `multer` high — the scariest-reading of the six — **was never reachable**, and that is the interesting part. | 1 | High severity, on the *upload path*, in a product whose main job is ingesting spreadsheets. There is no `FileInterceptor` and no `@UploadedFile` in the tree: ingestion takes base64 JSON, and multer ships inside `@nestjs/platform-express` without ever being invoked. It is fixed anyway. What matters is that both the API controller and the web upload page carry comments promising real multipart handling once the upload widget lands — that is the commit where this becomes live, and no one re-reads a risk document while adding a file picker. |
| **D-242** | Reachability verdicts are **enforced as tests, not left as prose**. | 1 | Every "not reachable" verdict rests on a fact about today's source — no `next/image`, no multipart handler, no direct `uuid` call. Those facts are true and will stop being true in some commit that has nothing to do with security. That is the same defect class as a metric defined twice: two sources of truth that agree now and drift silently later. `dependency-reachability.test.ts` goes red naming the CVEs the change just activated. Verified by adding a `next/image` import and watching it fail, which is the only reason to believe it. |
| **D-243** | The reachability guard searches **untracked files too**, and excludes itself. | 2 | `git grep` reads tracked files only, so a brand-new uncommitted component importing `next/image` was invisible — green locally, red in CI, which is safe but teaches people the test is noise. `--untracked` still honours `.gitignore`, so build output stays out. It then found a second problem immediately: the test file quotes the patterns it searches for and so matched itself. It had only been passing because it was itself uncommitted — an accident, not a result. |
| **D-244** | Bumping vitest in the root `package.json` alone **fixed nothing**, and the suite still went green. | 1 | The four workspaces each pinned their own `^2.1.1`, so npm installed a nested vitest 2 for each and `npm test` kept running the vulnerable version while `npm audit` kept reporting it. Only the suites invoked explicitly through `../../node_modules/vitest` used 4. One file changed, a green run, and nothing actually upgraded — the same shape as every vacuous test in this project, in dependency form. All five files now agree. |
| **D-245** | An npm `overrides` entry that does not take effect was **removed rather than left in place**. | 1 | postcss and sharp were pinned to patched versions to close the last three highs without a Next 16 major. npm refused both on a clean reinstall: Next pins postcss to an exact `"8.4.31"`, and sharp is an optional dependency it skipped. An override that reads as a fix and silently is not one is worse than no override — the next person greps `package.json`, sees it handled, and stops looking. |
| **D-246** | CI audit ratcheted to **enforcing at critical**, still reporting below it. | 2 | It was `|| true` on everything, with a note saying make it enforcing once the tree is clean. Critical is now zero, so the gate is green today and goes red the moment one appears. The three remaining highs stay non-failing because they are unreachable and accounted for — a gate that is already red when you add it is one people learn to ignore, which is what the previous version was avoiding. |
| **D-247** | **Next 16 is not mine to take — Tier 3, and it buys no security anyway.** | 3 | `npm audit` labels `next` high, which is misleading: it has **no advisory of its own** and is flagged solely because it depends on postcss and sharp. Upgrading would change a decided stack line (CLAUDE.md §3 names Next.js 15) and require re-verifying seven admin screens and both rep screens. Worth doing eventually for its own reasons; worth doing *for this* is a different claim, and it is not true. |
| **D-248** | **`db/schema.sql` could not be applied to an empty database.** Two indexes were written above the tables they index. | 1 | `ix_event_status` sat 100 lines above `order_status_event`, and `ix_activity_employee_day` 20 lines above `activity`. Postgres accepts neither on a fresh database. Both were introduced by my own performance-pass patch scripts, which inserted each index next to a *related index* instead of next to its *table*. Nothing caught it because every database anyone here has touched already had the tables — the statement succeeds, `migrate: ok` prints, and the defect is invisible until someone builds from nothing: a new environment, a restore, or a disaster. 681 tests and every CI run to date passed over it. The first attempt to bootstrap from empty hit both inside a minute. |
| **D-249** | The RLS suite and restore drill are **in CI, building from an empty database on every push**. | 1 | These are the tests that have actually caught things here — the isolation suite, the adversarial review, the policy-coverage guard, the velocity lock, the fan-out guard — and they ran only on one Windows laptop. The earlier note explaining their absence was honest but had become the reason a bootstrap defect could live in the repo indefinitely. The job migrates from nothing, seeds, seeds the cross-rep fixture, starts the API and runs all 87, then proves a restore keeps its policies. Rehearsed end to end locally against a throwaway database before a line of YAML was written, which is what surfaced D-248. |
| **D-250** | CI seeds the **cross-rep fixture**, and that step is not optional. | 1 | `db:seed` loads master data; `db:seed:dev` creates two reps holding different leads and a customer belonging to neither. Without the second, every isolation assertion passes **vacuously** — a rep who can see nothing looks exactly like a rep who is correctly scoped. An RLS suite that goes green while proving nothing is the worst possible outcome for this project, and it would have been one missing line away. |
| **D-251** | A **database-free schema-order test**, because the CI job is the slow way to learn this. | 1 | `schema-order.test.ts` scans `db/schema.sql` for any index, trigger or ALTER that references a table created later, and checks that every table `rls-policies.sql` secures actually exists. It needs no Postgres and runs in milliseconds, so it fails next to the edit that caused it rather than forty seconds into a CI job. Verified by reintroducing a forward reference and watching it name the exact line. |
| **D-252** | The RLS-coverage scan reads **both forms of enabling RLS**, and says so if it finds fewer than 25 tables. | 1 | The first version scanned only `ALTER TABLE … ENABLE ROW LEVEL SECURITY` and found 14 of 29 — the admin-only tables are secured inside a `FOREACH` over an array of names. It would have passed happily while checking half the schema. The floor assertion exists so the parser cannot silently degrade to one form again. Comments are stripped first, because one of them contains "colleague's" and a stray apostrophe derails any scan for quoted identifiers. |
| **D-253** | **The restore drill could not run on Linux.** It looked for `pg_dump.exe`. | 1 | `tool()` appended `.exe` unconditionally, so on any non-Windows host the drill reported "pg_dump was not found — set PG_BIN" and stopped. Invisible for as long as it only ever ran on the laptop it was written on; it failed the first time CI executed it, which is the whole argument for putting it there. The client's production database is Linux: **a disaster-recovery drill that cannot execute on the platform it is meant to recover is not a drill.** The RLS suite itself passed on that same run — 87 tests green in CI on the first attempt. |
| **D-254** | **CRITICAL - nothing in this system ran on a schedule.** Rule 6 was built, tested, and never called. | 1 | CLAUDE.md rule 6 is the only automatic movement in the product: untouched 48h alerts, 72h returns the lead to the pool. It was implemented in `FollowupService`, covered by unit and live-RLS tests, and exposed on an endpoint - and nothing invoked it. The only `setInterval` in the entire codebase was the rate limiter's memory sweep. In production a lead would have sat on one rep's list indefinitely, the repeat queue would never have materialised, and the daily digests would never have been sent. Four pieces of working, tested automation, all inert. Passing tests again proved the rule, not that the rule ever runs. |
| **D-255** | The scheduler lives in the **API**, not `apps/worker`. | 2 | The declared home for background work is BullMQ on Redis, and these four jobs are not queue-shaped: periodic maintenance, no payload, no fan-out, no retry semantics worth the machinery. Putting them in the worker meant either duplicating the recall SQL there - two sources of truth for rule 6 - or inventing service-to-service auth so the worker could ask the API to do it. It would also have meant nobody could exercise them without Redis, which is down on the only machine this is developed on. The job functions are plain and take a pool, so moving them later is a change of caller and nothing else. Redis stays for genuine queue work. |
| **D-256** | **The six materialised views were never refreshed.** Every report was serving the day the database was migrated. | 1 | docs/04: reports read certified views and nothing else, and six of those are MATERIALISED. The only `REFRESH` statement anywhere in the repo was inside the restore drill. So every KPI, funnel, product-revenue, geography, RTO and repeat-queue figure was frozen at migrate time - not wrong, STALE, and stale in the way that is hardest to notice: the numbers stay internally consistent and simply describe an earlier day. A dashboard would show a plausible yesterday forever and no test would fail. |
| **D-257** | Refreshing goes through **one SECURITY DEFINER doorway**, because `app_role` owns nothing. | 1 | `REFRESH MATERIALIZED VIEW` requires ownership of the view. `app_role` has none by design (D-21) and the matviews are REVOKEd from it outright, which is what stops a rep reading a colleague's KPIs. So the refresh cannot be a query the API issues. The rejected alternative was giving the API process migrator credentials - DDL rights over the whole schema, held by a web-facing service, to run a maintenance statement. `refresh_certified_views()` may refresh the certified views and do nothing else, with a pinned `search_path`. A test asserts that EXECUTE on it did **not** become SELECT on the views. |
| **D-258** | Scheduled jobs act as a **dedicated locked actor**, not as a real admin and not as the migrator. | 2 | The 72h recall writes to `lead_assignment` and `audit_log`, and those rows need an actor. Running as the migrator bypasses RLS entirely; running as one of the three real admins puts Sunita's name on work she did not do, which corrupts the audit trail in the quiet way that matters - the trail exists precisely to answer "who moved this lead". `system@razorveda.local` is role ADMIN so `is_admin()` accepts it and the job runs THROUGH the policies rather than around them; permanently `is_locked` so it can never authenticate; seeded with a hash of a random value the seed then discards, so no password exists to leak; and it has **no employee row**, so it cannot appear in a roster, hold a lead, carry a target or earn incentive. Four tests hold each of those properties. |
| **D-259** | Every job runs under a **Postgres advisory lock**. | 1 | Rule 9 says one API, but "one API" is a deployment fact rather than a guarantee: a Coolify rolling restart runs two instances for a few seconds, and that is long enough for two ticks to overlap. The recall UPDATE is `FOR UPDATE`-guarded so it would not double-return a lead, but the digest job has no such protection and would send every rep two copies of her morning plan. `pg_try_advisory_lock` - the non-blocking variant - so the second instance SKIPS rather than queueing up to run the digest again thirty seconds later. Observed working on the first real tick: a cold refresh took 19.7s and the tick behind it correctly skipped. |
| **D-260** | **I introduced a duplicate index and the fresh-build guard did not catch it.** Now it does. | 1 | Adding a unique index to `mv_rto_analysis` created a second `ux_mv_rto_analysis`, because the existing definition spanned two lines and the grep used to check for it only matched single-line ones - so it reported the index as absent when it was right there. Two `CREATE INDEX` statements with one name fail the whole bootstrap, and it would have passed locally against a database that already had the index. The schema-order test now checks duplicate object names across all three applied files, and that every matview has a unique index at all. Verified by reintroducing a duplicate. My original claim that the view lacked an index was simply wrong. |
| **D-261** | `NestFactory.create` is given the **ExpressAdapter explicitly**. | 1 | Adding `@nestjs/schedule` reshuffled npm's hoist layout: `@nestjs/core` stayed at the workspace root while `@nestjs/platform-express` moved under `apps/api/node_modules`. Nest DISCOVERS the HTTP driver with a bare `require` resolved from core's own location - so core could not see it, and the API died on boot with "please ensure to install @nestjs/platform-express", about a package that was installed and importable. A clean `npm ci` reproduced it exactly, so CI would have hit it too. Importing the adapter in `main.ts` resolves it from `apps/api`, where it is plainly visible, and removes the dependency on npm's hoisting decisions. Explicit beats discovered for something the process cannot start without. |
| **D-262** | `apps/web` is **built** in CI, not just typechecked. | 1 | 12 routes were typechecked and never bundled. `tsc` does not run the bundler, so a broken import path, an invalid route export or a client component importing server-only code all typecheck cleanly and fail at build - first discovered on the VPS, during a deploy. It builds clean today, so this is a guard rather than a fix. |
| **D-263** | The refresh is **measured at the client's volume**, not assumed. | 1 | 180,040 orders and 996,115 status events: 19.7s cold, then ~6s warm, all six CONCURRENT so no reader is ever blocked. A 15-minute interval is comfortable at roughly 6 seconds of work per 900. Recorded because "refresh the views periodically" is exactly the kind of decision that quietly stops being safe as data grows, and the next person needs the starting number to compare against. |
| **D-264** | **Playwright, four tests, covering the seams between the browser and the API.** | 1 | `apps/web` had zero behavioural coverage: twelve routes, typechecked and built, with nothing exercising them. Every other test in this repo proves the API or the database, and all of it can be perfectly correct while the login form posts to the wrong path, the session cookie is dropped, or the worklist errors because a field was renamed on the server. Those are the failures a rep meets in her first five minutes and no existing test can see. Deliberately few, and deliberately not a second suite for the business rules - those are tested where they live, against a database, in milliseconds. A browser test re-asserting an incentive figure would be slow, flaky and a second source of truth. |
| **D-265** | **A rep who opened an admin screen was served the entire admin shell.** Found by the first E2E run. | 1 | Navigating to `/security` as an EMPLOYEE rendered the full rail - all seven admin screens, each with a description of what it does - plus the page heading and its explanatory text, with an EMPLOYEE badge in the corner. No data leaked: the API refuses every request behind it and RLS refuses anything past that. What she got was an admin console with empty tables and no explanation, which reads as a broken product rather than a boundary, and breaks the definition of done - error states say what happened and what to do next. It also advertised the whole admin surface to someone with no business seeing it. The layout now checks the role and says so plainly, with a link back to her worklist. **Not a redirect**: bouncing her silently would leave a rep who followed a stale bookmark wondering what she did wrong. |
| **D-266** | The role check is written as **"is not an admin"**, not "is an employee". | 2 | Two roles today plus OWNER (rule 7). Phrasing the guard as a denylist of one known role would admit any role added later by omission; phrasing it as an allowlist of ADMIN and OWNER refuses by default. The same reasoning as the append-only tables: the safe direction should be the one you get when you forget. |
| **D-267** | The E2E assertion checks a **structural property**, not a phrase. | 1 | The first version grepped the page for words like "forbidden" and "not allowed", and failed against a perfectly clear message that happened not to use them - it was testing the copywriting. What must be true is that no admin surface renders, so it now asserts the admin rail is absent, plus that a heading and a route back exist so a blank page cannot pass instead. |
| **D-268** | `[role="alert"]` matches a **Next.js framework element**, and cost four false failures. | 1 | Next's App Router injects its own `role="alert"` route announcer into every page for screen readers. It is always present and always empty, so a bare selector matches it and every assertion about error messages becomes an assertion about the framework. The first read of the failure looked like the worklist rendering an error for a rep who had just signed in. Filtering on non-whitespace text is what makes the selector mean "the app told the user something". |
| **D-269** | E2E runs as its **own CI job**, with Chromium only. | 2 | Separate from the rls job so a browser failure is never mistaken for an isolation failure, and so the two run in parallel rather than serialising a browser download behind the database suite. Chromium alone: Firefox and WebKit would triple the download to re-test the same four seams in browsers the client's team does not use. Locally `PLAYWRIGHT_CHANNEL=chrome` uses the Chrome already installed, because a 130 MB download to run four tests is how a suite stops being run. |
